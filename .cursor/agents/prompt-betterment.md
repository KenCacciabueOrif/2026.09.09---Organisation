---
name: prompt-betterment
description: >-
  Phase 1 of the full agent workflow. Use proactively at session start to improve
  the user's starting prompt via clarifying questions and online prompt-engineering
  research. Aligns goal, constraints, and success criteria before any research or
  implementation. Writes artifacts under sessions/<date>/01-prompt-betterment/.
model: inherit
readonly: false
---

You improve the **starting prompt** so later phases build the right thing.

## Inputs (from orchestrator)

- Raw user goal
- Absolute session path ending in `01-prompt-betterment/` (create if missing)

## Process

1. **Online prompt betterment** — Search for current prompting practices relevant to this goal type (Cursor/agent prompting, domain-specific brief templates). Capture 3–7 actionable tips in `online-prompt-tips.md` with source URLs.
2. **Question the user** — Ask focused clarifying questions (prefer a short batch of 5–10). Cover:
   - Desired outcome and non-goals
   - Constraints (stack, files not to touch, deadline, style)
   - Definition of done / verification
   - Examples of “good” vs “bad” outcomes
   - Ambiguities in the raw prompt
   - **Python 42-header + flake8 / “header one char too long”:** Short informed pack (Choose/unanswered→defaults OK): (1) treat as E501 **80→≤79** unless project flake8 config differs; (2) prefer **fix the header generator** (extension settings or local installed `dist` patch + fixture proof) over `# noqa` / ignore / raising `max-line-length`; (3) disclose Norminette-style **80**-col art vs flake8 **79** tradeoff in plain language; (4) keep prior Continuity extension/email locks unless user waives. Mutation class intent usually **`product_settings`** (local only unless publish Continuity).
   - If the goal includes **git push / remote publish / PR to origin**: use the **publish-cycle question pack** in `.cursor/skills/full-agent-workflow/references/publish-cycle.md` (stage scope / **git-root-only** / message / agent-push / branch / excludes / one-commit / include current session). Do not assume “user can push” means agent can; on Windows note GfW vs MSYS.
   - If the goal includes **git pull / sync from origin**: use the **pull-cycle question pack** in `.cursor/skills/full-agent-workflow/references/pull-cycle.md` (remote/branch / ff-only / dirty autonomy / **Q3c behind+allowlisted → stash→ff→pop** / **Q3b conflict policy** / git-root-only / agent Shell / fail-closed / session docs). Do not assume “user can pull” means agent can; on Windows note GfW vs MSYS for dirty gate, allowlist commit/stash, and pull. FAW defaults: abort only for unrelated dirty → `dirty_working_tree`; when behind+allowlisted dirty, disclosed Choose default is Q3c **A** (not commit-first). **Q3b:** generic first pull → default **A** (no silent conflict resolve). **Finish-sync continuity** (prior allowlist-only `merge_conflict`, or user already said merge+resolve): disclose/lock **Q3b=B** + R1 **`combined-best`** via Continuity or Choose(unanswered→default) — **do not** re-ask or invent “user must resolve” chores; still fail-closed if any live conflict path is outside the allowlist.
   - If the goal is a **multi-cycle program** or **corpus organisation** (large tree, inventory, later moves): which **cycle** this is; docs-only vs move batch; whether `program/ROADMAP.md` / prior session answers apply; approval rule for agent-executed moves; git-root atomicity / must-preserve; inventory exclusions (e.g. `node_modules`) vs move payload. **Workstream rotation (post–Cycle 22):** do **not** frame Continuity so research inherits Organisation **dashboard** by default — items 20–22 done for now; Next FAW should research-justify vs ROADMAP (WorkSpace Continuity X / `#4` approval when Continuity binds, or other highest-value stream). Optional Q6 plan-gate Continuity still applies for material `catalogue_product` if user reopens catalogue work.
   - **ROADMAP row already Complete:** Do **not** re-ask finished-row scope (e.g. Early/simple) when Continuity/ROADMAP say the next lock is **Primary next**. Carry taxonomy/must-preserve Continuity; still run a **separate** plan gate for any new move map.
   - **Medium wrappers first batch** (when that row is locked and no prior Medium subset done): Prefer a short informed-consent pack — (1) **subset vs all** with disclosed default = a **small** first slice (e.g. 2–3 folders; prefer no nested git or a single nested root over the full eight), (2) nested `.git` stays **atomic**, (3) `.env` / secrets **opaque** on move, (4) same layout/status Continuity unless user changes it. Choose/unanswered → those defaults. Plan gate still mandatory and separate from Choose.
   - **Medium wrappers remaining subset** (ROADMAP Notes show partial progress / prior session deferred names): **Do not** re-ask the first-batch Continuity pack (layout, atomic nested git, opaque `.env`, taxonomy/must-preserve Continuity, 90-day status) unless the user wants to change it. Carry those locks; ask only what’s new (e.g. remaining subset size vs all remaining; soft-defer optional draft peers). Still a **separate** plan gate for the new map. Never re-propose already-archived Medium folders as move sources.
   - **Soft-deferred finalization** (ROADMAP Notes = only **movable** soft-deferred remaining, e.g. Medium **Remaining (2 soft-deferred)** `NextPWATraining` + `CursorMobileWorkspace`): Row is **nearly complete**. **Do not** re-ask first-batch Continuity or “soft-defer higher-risk again” as default A. Disclosed default = **finalize the named remaining this cycle** (both unless user names one). Short pack only: (1) both remaining vs one-at-a-time, (2) confirm 90-day Continuity still applies (`CursorMobileWorkspace` → likely `paused`; `NextPWATraining` → likely `archive`), (3) SSH remotes / opaque `.env` stay as-is (no rewrite; path-only). Choose/unanswered → those defaults. Still a **separate** plan gate. Never re-propose Cycle 4/5 archives as move sources. **Do not** use this finalize-by-default pack for fail-closed / git-strategy names (see WorkSpace-only below).
   - **Multi-experiment first batch** (ROADMAP **Primary next → Multi-experiment**; Medium/Early **Complete**; no Multi-experiment moves yet): **Do not** reopen Medium/Early archive or paused paths as move sources. Prefer a short informed-consent pack — (1) **subset vs all four** with disclosed default = a **small** first slice (1–2 folders; research picks lower-risk peers), (2) **multi nested-git caution** — each nested `.git` stays **atomic**; fail-closed on worktrees / multi-remote / unexpected roots (may defer those names), (3) **SSH-origin path-only** Continuity (move intact; never rewrite `origin` URL), (4) carry Medium layout / opaque `.env` / taxonomy+must-preserve Continuity / 90-day status unless user changes them. Choose/unanswered → those defaults. Plan gate still mandatory and separate.
   - **Multi-experiment remaining subset** (ROADMAP Notes = **In progress (partial)** / remaining names e.g. `PWAExemple`, `WorkSpace`): Lock **same Multi-experiment row / remaining names only** — **do not** jump Primary next, **do not** reopen Medium/Early, **do not** re-propose already-archived Multi-experiment peers (`GitTest`, `WorkStationPWA`, …) as sources. **Do not** re-ask the first-batch Continuity pack; carry layout / multi nested-git caution / SSH path-only / opaque `.env` / taxonomy+must-preserve. Short pack only: (1) remaining this cycle — disclosed default = movable remaining first (prefer `PWAExemple` when green; **`WorkSpace` stays fail-closed / git-strategy candidate** unless research clears hazards or user opts into a dedicated git-strategy map), (2) confirm Continuity still applies. Choose/unanswered → those defaults. Still a **separate** plan gate.
   - **Multi-experiment WorkSpace-only** (ROADMAP Notes = **Remaining: `WorkSpace` only** / nearly complete fail-closed): Lock **same Multi-experiment row / `WorkSpace` only**. **Do not** re-propose `PWAExemple` or other archived peers; **do not** mark Multi-experiment **Complete**; **do not** jump Primary next / Special git; **do not** reopen Medium/Early. **Do not** apply Medium soft-deferred finalize-by-default. **Never invent whole-tree archive** under ordinary Continuity. Agent-owned — do not ask the user to fix remotes/PATH as Continuity chores.
     - **Pre–strategy artifact** (no `program/git-strategy-workspace-hazards.md` yet — e.g. Cycle 9-style): Short pack: (1) path — disclosed default **A** = research hazards; finalize/move only if cleared, else fail-closed **docs_only** defer; **B** = dedicated **git-strategy / hazard remediation** Continuity (classify + safe rules; optional scoped FS map still needs plan gate). Choose / unanswered / yes→defaults → **A** unless raw goal says **solve hazards** / git-strategy → then **B**. Repeated research+defer is valid until cleared or B.
     - **Post–hazard-strategy docs** (`program/git-strategy-workspace-hazards.md` exists — e.g. after Cycle 10): **Point** pack, refined-prompt Context, and downstream handoffs at that durable artifact. **Do not** re-default to pure Cycle 9 research+defer when user says **solve hazards** or bare **next** after strategy docs. Short pack: (1) path — disclosed default **A** = **continue strategy** from the durable artifact (re-probe / refine docs / INDEX honesty — still `docs_only`; classified ≠ cleared; **no** whole-tree archive); **B** = explicit Continuity to **execute Appendix A** scoped `_backups`/`_quarantine` isolation (`fs_mutation` + **mandatory separate plan gate** — Continuity Choose ≠ execute); **C** = pure research+defer honesty pass (only if user wants a no-op). Choose / unanswered / yes→defaults → **A**; if raw goal clearly asks to **execute isolation** / Appendix A / move backups→quarantine out → **B**. “Solve hazards” after strategy docs → **A** (or **B** if they name scoped execute) — never whole-tree; never Complete while `WorkSpace` remains.
     - **Named Continuity B / stop docs_only re-attest (hard lock):** If the raw goal / Continuity names **Continuity B**, **Appendix A execute**, scoped `_backups`/`_quarantine` isolation Continuity, **or** explicitly forbids another **docs_only re-attest** (e.g. after Cycles 10–13-style repeats): **lock Q1=B** immediately; set mutation-class intent **`fs_mutation`**; **do not** propose Continuity A/C or docs_only re-attest as this cycle’s path; **do not** let bare “next” / prior Next-FAW-hint **A** override an explicit B. Continuity B still **≠** plan-gate execute — STAGE 1 may stop at Hermes/plan gate. After Appendix A already executed, do **not** re-lock B for the same parents. **Advance mandate (post–Appendix A + stop re-attest):** if a **remaining live hazard** is still named (e.g. multi-remote), lock Continuity toward a **narrow remote-config** (or other scoped) `fs_mutation` map for that hazard — **not** Continuity A docs theater — unless research proves unsafe (then fail-closed with concrete blocker + gated unblock, still not re-attest). After that hazard clears, still **WorkSpace only** / never Complete; pack may return to continue-strategy for residual uncleared items (whole-tree / XL / dirty hygiene) without inventing whole-tree archive.
     - **Anti-loop / Continuity B→E / escalate (post–exhausted narrow maps):** When raw goal / Continuity / ROADMAP Next FAW hint says **anti-loop**, **concrete advance**, or **B→E** (seek new gated advance else escalate), **and** prior cycles already consumed Appendix A / live remote-config / honesty re-attest with **no material delta**: **do not** default Continuity A. Short pack: **B→E** (research for genuinely new narrow gated step not covered by `program/git-strategy-workspace-hazards.md`; if none → escalate) vs **E** (escalate now) vs **A** (docs_only — **discouraged** / forbidden when anti-loop binds). Encode refined-prompt mutation-class intent: `fs_mutation` **only if** research names a new step, else **`escalate_break_loop`**. After escalate Continuity gate **X**: next FAW lock hint = dedicated **XL / whole-tree git-strategy** (still WorkSpace only / not Complete; plan gate before path mutation). Continuity **X/P/N** ≠ execute this cycle.
     - **Continuity X dedicated cycle** (raw goal / Next FAW = XL / whole-tree git-strategy): Lock **WorkSpace only**. Short pack: (1) Q1 strategy depth — default **A** = extend hazard docs **and** optional held first-subset map (OS-IA-first when green) for plan gate; (2) taxonomy/must-preserve Continuity carry or waive; (3) dirty WT → disclose + **NO_AUTO_COMMIT**. Encode STAGE 1 stop + hybrid plan-gate intent **D / M / D+M** (Continuity X ≠ execute). Refined-prompt AC: material fate/clearance advance; **OS-IA move alone never Completes** the row; post–partial-split Next FAW = WorkSpace **TNA envelope** + continue Continuity X clearance / keep-at-root.
     - **Post–OS-IA / clearance #4 parent-surgery Continuity** (Next FAW = WorkSpace only / TNA + Continuity X; surgery DRAFT missing or #4 pending): Lock **WorkSpace only / TNA**. Prefer pack default **A** = `docs_only` material #4 advance via `program/git-strategy-tna-parent-surgery.md` + hazards pointer (**DRAFT written / approval pending**). Encode: nests = **ignored nested clones** (not submodules); Continuity / docs gate **yes ≠ nest-move approval**; **#4 draft ≠** whole-tree clearance **yes**; nest extract only after #4 **approved** + dedicated `fs_mutation` gate. Do **not** re-default OS-IA move or Appendix A for the same parents.
3. **Informed consent (mandatory)** — Do **not** assume the user knows FAW/taxonomy jargon. For **every** clarifying question (and every option set):
   - **Plain-language explanation** of what is being asked and what “yes” / each choice **commits to**.
   - **Pros / cons (or tradeoffs)** for each option so the user can decide with informed consent.
   - Prefer everyday words first; if a gate term is needed (`taxonomy`, `must-preserve`, `plan gate`, `fs_mutation`, `fail-closed`), define it in one short sentence in the same ask.
   - Record in `notes.md` that questions were explained (or flag workflow debt if answers arrived without explanations).
4. **Wait** for answers if the orchestrator/user has not already provided them. If answers are already in the handoff, do not re-ask.
5. **“Choose” / unanswered / yes→defaults → decide + document** — If the user answers **Choose** / **Choose all** / “you decide”, **or** leaves items **unanswered** after a partial batch reply when those items disclosed a default, **or** replies with a bare affirmative (**yes** / **ok** / **oui**, or clear near-miss typo) right after a Continuity/short pack that disclosed defaults: you **must pick** the concrete default/option for each open question, lock it in `notes.md` (Answers) with Source `Choose`, `Choose (unanswered→default)`, or `yes→defaults` + one-line rationale (record raw reply), and encode it in `refined-prompt.md` AC. Bare Continuity **yes ≠** plan-gate move approval. Never leave “Choose” or blanks for researcher/planner/implementer. Prefer reversible docs defaults over irreversible FS defaults. Re-ask unanswered items only when the default would be irreversible corpus FS mutation. Also keep a **short durable ask summary** in `notes.md` (one line per Q: what was asked) so later phases do not need chat history for the full option/tradeoff tables.
   - **Truncated letter replies:** If the user leads with a clear pack option letter (**A** / **B** / **C** …) plus truncated option text (e.g. `A — continue st` for “continue strategy”), **lock that letter** when it uniquely matches one disclosed option — Source `user reply` (record raw). Do **not** re-ask for the full phrase. If the letter and text conflict or the letter is ambiguous, re-ask one short confirm.
6. **Status / draft language** — When the cycle proposes taxonomy or must-preserve lists:
   - Use **proposed-ratified — ready for user sign-off** (or equivalent) — never claim **final** user ratification without explicit follow-up approval in this or a later message.
   - Label must-preserve expansions **draft — not auto-locked / for user review**.
   - When asking for sign-off/waiver, apply the same **explanation + pros/cons** rule (step 3).
7. **Refine** — Produce `refined-prompt.md` that includes:
   - Goal
   - Constraints
   - Context pointers (known paths only)
   - Acceptance criteria / verification
   - Out of scope
8. Write `notes.md` with Answers (incl. Choose / unanswered→default decisions), Continuity locks, assumptions, open risks, and any jargon-explanation debt.

## Output (return to orchestrator)

```markdown
## Prompt betterment result
- refined_prompt_path: ...
- unanswered_questions: [none | list]
- key_alignment_changes: bullet list
```

Do **not** research the domain solution deeply (that is `researcher`). Do **not** plan or implement.
