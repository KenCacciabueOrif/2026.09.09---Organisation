# Implementation log — 2026.09.14-1004

## Status

`complete` (product_settings)

## Baseline (step 1)

- Fixture `C:\Project\current\ft_prework\ex0\ft_first_exception.py`: header lines 1–11 all **len=80**; By: already full `kcacciab <kcacciab@student.42lausanne.ch>` + `>`.
- Extension folder: `C:\Users\CaDa\.cursor\extensions\ensui-dev.42header-multicampus-0.42.16-universal`
- Provider: `ensui-dev.42header-multicampus` **enabled** in `extensions.json`; `kube.42header` folder remains on disk but **not** enabled (not re-enabled).
- Settings unchanged: `42header.username=kcacciab`, `42header.email=kcacciab@student.42lausanne.ch` (no width key; email not shortened).
- Installed package has **no** `src/header.ts` (dist-only ship) — see Deviations.

## Patch (step 2)

**Backup:** `...\dist\extension.js.pre-79-backup` (pre-patch copy).

**File:** `...\dist\extension.js`

**Edits:**
1. `genericTemplate` (`Ht=\`...\`.substring(1)`): every frame line **80 → 79** by removing **one** padding/border column immediately before the closing `*` (full-`*` borders: remove one `*`). **`$AUTHOR` + 36 underscores = field width 43** unchanged.
2. `extractHeader` regex: `.{80}` → `.{79}` (single occurrence; quantifier `{10}` unchanged).

**Post-patch verify:**
- Template non-empty lines: 11 × len=79
- `.{80}` count = 0; `.{79}` count = 1
- AUTHOR underscores = 36 → field **43** (≥41)

## Src note

`src/header.ts` **absent** in install (only `dist/extension.js` + package metadata). Dist-only patch. Upstream reference remains https://github.com/ensui-dev/vscode-42header-plus `src/header.ts` for future installs that ship src.

## Reload (step 3)

**Soft tip (user):** run **Developer: Reload Window** so Cursor loads the patched `dist/extension.js` for live insert/update-on-save.

**Agent regenerate:** did **not** invoke Command Palette Reload. Fixture was regenerated via **template-math equivalent** of ensui `renderHeader` (Python delimiters `["# "," #"]`, field pad widths from patched `Ht`, same fill rules). Matches plan allowed verify path when Reload UI unavailable.

## Fixture regenerate (step 4)

Path: `C:\Project\current\ft_prework\ex0\ft_first_exception.py`

- All header lines **len=79**
- By: `#    By: kcacciab <kcacciab@student.42lausanne.ch>  +#+  +:+       +#+        #` — full email + `>` present
- Created timestamp preserved (`2026/09/14 09:59:52`); Updated bumped at regen time
- Exercise body below header: none (file is header-only aside from trailing blank)

## flake8 (step 5)

- Soft-install: `python -m pip install --user flake8` → **flake8 7.3.0** (was missing)
- Command: `python -m flake8 C:\Project\current\ft_prework\ex0\ft_first_exception.py` (default max 79; no project config)
- Result: **no E501** on header lines
- Noted out of scope: `W391 blank line at end of file` (line 12) — pre-existing style / not introduced as header width fix

## Zero corpus / ROADMAP attestation (product_settings)

- **Wrote:** local ensui `dist/extension.js` patch + backup; fixture header regenerate; this session `04-implementation` docs; user-scope flake8 install
- **Did not mutate:** `program/ROADMAP.md`, WorkSpace, corpus catalogue moves/renames/deletes, org-repo publish/push, User `settings.json` email/username, kube enablement

## Re-apply after extension update

Marketplace/Open VSX update of `ensui-dev.42header-multicampus` **overwrites** `dist/extension.js` and clobbers this patch.

1. Confirm version folder under `%USERPROFILE%\.cursor\extensions\ensui-dev.42header-multicampus-*`
2. Re-apply the same two edits in `dist/extension.js`:
   - Shrink each `Ht` template line from 80 to 79 **without** reducing `$AUTHOR`+underscores below 43 (prefer keep 43)
   - Change `extractHeader` `.{80}` → `.{79}`
3. Optionally restore from `extension.js.pre-79-backup` only if still matching the installed version; otherwise re-edit the new stock file
4. **Reload Window**, then re-insert/update headers on files as needed

## Rollback

1. Restore stock bundle: copy `extension.js.pre-79-backup` → `extension.js`, **or** reinstall `ensui-dev.42header-multicampus` from Marketplace/Open VSX
2. Reload Window
3. Restore fixture header from prior session art / re-insert with stock 80-col generator if desired

## Soft remaining (user)

- **Reload Cursor window** so future UI-driven header insert/update uses the patched bundle (fixture already regenerated via template-math; Reload does not block AC complete)

## Deviations from plan

- Dist-only patch: no `src/header.ts` in install (logged; not a failure).
- Fixture regenerated by template-math render equivalent rather than ensui command after Reload (allowed by plan when command path unavailable).
- Temporary `_regen_header.py` used then deleted; procedure captured in this log.
