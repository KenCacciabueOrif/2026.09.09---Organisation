# Prompt betterment notes — Cycle 23

**Session:** `sessions/2026.09.15-1432/01-prompt-betterment/`  
**Prior (complete, do not resume):** `sessions/2026.09.15-1340/` (Cycle 22, final `5442730`) — dashboard items 20+22 done for now; Next FAW must research-justify vs ROADMAP (never inherit dashboard).  
**Consent UX:** All clarifying Qs include plain-language explanation + pros/cons. Explanations shipped with this pack (no jargon-debt for unanswered state).  
**Phase 1 status:** **complete** — Q1–Q5 locked **A** (Source: `user reply`). Continuity ≠ plan-gate execute.

---

## Durable ask summary (one line per Q)

1. **Q1 path** — WorkSpace Continuity X / #4 material advance-or-escalate (default A) vs escalate-first (B) vs allow other ROADMAP row if research proves higher value (C)?
2. **Q2 taxonomy / must-preserve** — CARRIED locked **A** (waive re-litigation) unless user changes.
3. **Q3 dirty TNA** — CARRIED locked **A** (disclose + **NO_AUTO_COMMIT**) unless user changes.
4. **Q4 plan-gate reminder** — Confirm Continuity/Choose = intent only; separate plan-gate yes still required before execute (ack default A)?
5. **Q5 publish** — After successful implement (if any): allowlisted mid+final git-manager via GfW/GCM (default A) vs session docs only (B)?

---

## Clarifying questions (user-facing pack)

### Q1 — Cycle 23 Continuity path (blocking)

**Plain language:** Cycle 22 finished the organisation **dashboard** workstream for now. This cycle must **not** reopen dashboard by habit. The roadmap’s unfinished Multi-experiment item is still **`WorkSpace` only** (a folder at `C:\Project` with nested git projects inside a dirty “TNA” parent). Cycle 20 wrote a **draft** parent-surgery plan called clearance **#4** — “draft” means written for review, **not** approved to move nested projects out. Whole-tree archive is **forbidden**. Standing anti-loop rule: each cycle must make a *real* advance — or stop and escalate — not restate old docs.

What should Cycle 23 lock as its Continuity path? (Saying **Choose** / leaving blank / bare **yes** locks the **default**.)

| Option | What it commits to | Pros | Cons / tradeoffs |
| --- | --- | --- | --- |
| **A (disclosed default)** | Provisional lock **Multi-experiment / WorkSpace only / Continuity X**. Researcher must **justify** this vs other ROADMAP rows and pursue a **material #4 advance** (e.g. refine surgery doc to approval-ready + present **#4 approval** at plan gate, or a concrete gated next step named in research). If research finds **no material delta** vs Cycles 18–20 (and Cycle 22’s rotation note) → mutation-class intent **`escalate_break_loop`** (not empty re-attest). Nest / TNA path moves **only if** #4 is **approved this Continuity** **and** a dedicated `fs_mutation` plan gate later — Continuity yes ≠ nest execute. | Aligns with Cycle 22 Q3=A lock hint; highest programme priority still open; anti-loop baked in. | May end in escalate, not nest move; #4 approval is a real user decision. |
| **B** | **Escalate-first** this cycle — mutation-class intent **`escalate_break_loop`**. Research only enough to confirm no higher-value non-theater step; Continuity gate options for next FAW; skip implementer if `ready_to_implement: no`. Still **never** dashboard; still WorkSpace row **not** Complete. | Fast honest stop; no #4 theater. | No clearance/#4 progress this cycle. |
| **C** | Allow researcher to pick **another ROADMAP row** only if evidence shows **higher value** than WorkSpace/#4 (e.g. Special git / Hygiene / git-strategy-before-Obsidian). **Never** dashboard. If none higher → fall back to **A** logic (material #4 or escalate). | Keeps rotation honest if WorkSpace is stuck. | Diverts from Primary next; must still fail-closed on WorkSpace Complete. |

**Choose / unanswered / bare yes → default A.**  
**Truncated letter replies** (e.g. `A — WorkSpace #4…`) lock that letter when unambiguous.

---

### Q2 — Taxonomy / must-preserve (CARRIED — do not re-ask unless you want to change)

**Locked A (orchestrator / Cycle 22 Q2=A style):** carry Continuity — **waive** re-litigation this cycle. Taxonomy stays **proposed-ratified — ready for user sign-off** (not claimed final). Must-preserve stays **draft — not auto-locked**.

**Plain language:** We will not reopen the big “is the folder classification final?” debate just to advance WorkSpace/#4.  
**Tradeoff:** Faster Continuity X work vs delaying formal taxonomy sign-off (still available later).

---

### Q3 — Dirty TNA working tree (CARRIED — do not re-ask unless you want to change)

**Locked A:** disclose dirty TNA working tree; **NO_AUTO_COMMIT** / no stash-as-cleanup of that tree.

**Plain language:** Agents must not auto-commit or “clean” the huge dirty TestNewWorkspaceAgent tree as a side effect.  
**Tradeoff:** Safer vs slower (any future nest move needs #4 **approved** + dedicated plan gate first).

---

### Q4 — Plan-gate reminder (ack)

**Plain language:** Your Continuity / Choose answers only lock **intent** for research and planning. Before any nest move, remote-config, or other gated execute, you still get a separate written **plan** and must say **yes** (with plain-language + tradeoffs). Continuity **yes ≠** plan-gate execute.

| Option | Pros | Cons |
| --- | --- | --- |
| **A (default) Acknowledge** | Keeps dual-gate safety. | Extra pause before execute. |
| **B** | (Not offered) — cannot waive plan gate for nest/`fs_mutation`. | — |

**Choose / unanswered / yes→defaults → A.**

---

### Q5 — Publish after successful implement (allowlisted)?

**Plain language:** If this cycle implements org-repo docs/code (or later `fs_mutation` after gates), should the agent **stage / commit / push** allowlisted paths via Git for Windows + credential manager (mid + mandatory final closing pass)?

| Option | Pros | Cons |
| --- | --- | --- |
| **A (default) Yes** — dual mid+final git-manager; GfW/GCM; fail-closed if agent push blocked. | Session closes clean; FAW law. | Needs agent git auth (user terminal success ≠ agent ready). |
| **B Session docs only** — no agent push this cycle. | Safer if auth unsure. | Leaves allowlisted dirt; may block `complete`. |

**Choose / unanswered → A.**

---

## Answers

| Q | Decision | Source | Rationale |
| --- | --- | --- | --- |
| Q1 | **A** — provisional lock Multi-experiment / WorkSpace only / Continuity X; researcher must justify vs other ROADMAP rows; material #4 advance or `escalate_break_loop`; never dashboard | **user reply** | Raw locks Q1=A explicitly |
| Q2 | **A** — waive taxonomy/must-preserve re-litigation | **user reply** (carried) | Raw: Q2=A carried |
| Q3 | **A** — dirty disclose **NO_AUTO_COMMIT** | **user reply** (carried) | Raw: Q3=A carried |
| Q4 | **A** — Continuity locks intent only; separate plan-gate yes required before execute | **user reply** | Raw: Q4=A ack |
| Q5 | **A** — dual mid+final git-manager publish, GfW/GCM, fail-closed | **user reply** | Raw: Q5=A |

**Raw user Continuity reply:** `Q1 = A (provisional lock Multi-experiment / WorkSpace only / Continuity X; researcher must justify vs other ROADMAP rows, material #4 advance or escalate_break_loop — never dashboard). Q2 = A (carried). Q3 = A (carried, NO_AUTO_COMMIT on dirty TNA). Q4 = A ack (Continuity locks intent only; separate plan-gate yes required before execute). Q5 = A (dual mid+final git-manager publish, GfW/GCM, fail-closed). Proceed: researcher justifies workstream vs ROADMAP, then planner, then present plan gate.`

**Proceed order (user):** researcher → planner → present plan gate. Continuity ≠ plan-gate execute.

---

## Continuity (locked — do not re-ask)

| Lock | Value |
| --- | --- |
| ROADMAP row | **Primary next → Multi-experiment** — remaining **`WorkSpace` only** (TNA envelope + nests); researcher **must justify** vs other ROADMAP rows; may escalate if no material delta — **never** dashboard |
| Row status | **In progress / not Complete** — never Complete while WorkSpace remains; never jump Primary next / Special git while it remains |
| Strategy artifacts | `program/git-strategy-workspace-hazards.md` + `program/git-strategy-tna-parent-surgery.md` (#4 **DRAFT written / approval pending**) |
| Dashboard | **Done for now** (Cycle 22) — **forbidden** as default/inherited topic |
| **Q1** | **A** — WorkSpace Continuity X / material #4 or escalate |
| **Q2** | **A** taxonomy/must-preserve waive |
| **Q3** | **A** dirty TNA disclose **NO_AUTO_COMMIT** |
| **Q4** | **A** Continuity ≠ plan-gate execute (ack) |
| **Q5** | **A** dual mid+final publish GfW/GCM fail-closed |
| Nests | Ignored nested clones (not submodules); docs/#4 approval **≠** nest-move approval; #4 draft **≠** whole-tree clearance **yes** |
| Forbidden | Empty re-attest; re-propose Appendix A same parents; reopen OS-IA / Medium / Early / archived Multi peers; silent whole-tree archive; mark Multi-experiment Complete; nest extract without #4 **approved** + dedicated `fs_mutation` gate; inherit dashboard |
| Anti-loop (standing) | Material advance each cycle; if every remaining workstream would repeat prior findings → **`escalate_break_loop`**, not theater |
| Prior session | `sessions/2026.09.15-1340/` **complete** — **new cycle**, do not resume |
| Proceed | researcher justifies → planner → **present plan gate** |

---

## Assumptions

- Workstream rotation post–Cycle 22 binds: research must **justify** WorkSpace/#4 vs ROADMAP (or escalate); do not inherit catalogue/dashboard.
- #4 status unchanged entering this cycle: **DRAFT written (Cycle 20) / approval pending**.
- Mutation-class hint (Q1=A locked): **`docs_only`** for #4 approval-path advance; **`fs_mutation`** only if #4 approved + dedicated nest map gated; **`escalate_break_loop`** if research finds no material delta.
- STAGE shape: Continuity locked → research → plan → **plan gate** (normal); Continuity ≠ execute.

---

## Open risks

- Re-attesting #4 draft without material delta → anti-loop violation (must escalate).
- User mistakes Continuity / Choose for nest-move approval.
- Researcher must still compare WorkSpace/#4 vs other ROADMAP rows for honesty; Q1=A does not waive justification — only locks the Continuity path if justified (or escalate).
- Agent publish (Q5=A) may hit `agent_environment` / credentials — fail-closed session `blocked`, still run self-improver + final git attempt.

---

## Self-improvement backlog (if any)

- None this phase (questions include plain-language + pros/cons).

---

## Key alignment changes (raw → refined)

- Raw “next organisation FAW” + Cycle 22 lock hint → Continuity X / WorkSpace only / #4 **or** escalate / rare other ROADMAP — **explicit never dashboard**.
- Material-advance mandate encoded as AC + anti-loop exit.
- Dual gate: Continuity intent vs plan-gate execute kept separate.
- Taxonomy + dirty TNA carried; Q1–Q5 all locked **A** (user reply).
