# Prompt betterment notes

## Clarifying questions

For each question, record (or paste the user-facing ask):

1. **Plain-language explanation** — what is being asked and what “yes” / each choice commits to.
2. **Options** — with **pros / cons (tradeoffs)** per option (or for yes/no).
3. Do not assume jargon literacy; define gate terms in one short sentence if used.

If the user said **Choose** / **Choose all**, or Continuity bare **yes→defaults**, keep at least a **one-line ask summary per Q** here (even if full option tables lived in chat).

## Answers

(Include agent decisions when the user said **Choose** / **Choose all**, Continuity bare **yes→defaults**, or when items were **unanswered** after a partial batch — concrete option + Source `Choose` / `Choose (unanswered→default)` / `yes→defaults` + one-line rationale; record raw reply for yes→defaults.)

## Continuity (locked — do not re-ask)

(For publish cycles: git root path; stage scope; agent-push required?; remote/branch; one-commit?; include current session?; excludes.)

(For pull / finish-sync: Q2; Q3b; R1. After prior allowlist-only `merge_conflict` or user said merge+resolve, Continuity may already lock **Q3b=B** + **`combined-best`** — do not re-ask.)

(For program / corpus: locked ROADMAP row; if prior row **Complete**, next = **Primary next →** — do not reopen finished Early/simple. After Medium **Complete**: lock **Multi-experiment** — small first subset; multi nested-git caution; SSH-origin path-only (no rewrite). If multi-batch / Multi-experiment **in progress**, lock **remaining** names only (carry Continuity; do not re-ask first-batch pack; WorkSpace may stay fail-closed / git-strategy). If Multi-experiment **`WorkSpace` only** (fail-closed / git-strategy — **including after a prior docs_only defer process pass**): lock that name only — do **not** re-propose `PWAExemple`; do **not** mark Complete / Primary next / Special git; default = research+defer unless cleared or dedicated git-strategy (not Medium finalize-by-default); **repeated research+defer is valid**. If only **movable soft-deferred** remain (nearly complete): finalize those names by default — do not re-soft-defer as default A. Medium wrappers: subset default, nested git atomic, `.env` opaque; remaining Medium → carry Continuity, do not re-ask first-batch pack.)

## Assumptions

## Open risks

## Self-improvement backlog (if any)

(e.g. questions asked without explanations → flag for phase 06)

## Key alignment changes (raw → refined)
