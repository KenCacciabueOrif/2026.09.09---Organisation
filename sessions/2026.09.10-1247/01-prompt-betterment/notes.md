# Prompt betterment notes — Cycle 12 Multi-experiment (`WorkSpace` only / continue strategy)

Session: `sessions/2026.09.10-1247/01-prompt-betterment/`  
Role: **Cycle 12 — Multi-experiment WorkSpace-only, post–hazard-strategy Continuity**.  
Roadmap row (orchestrator-locked — **do not re-pick / do not jump Primary next / do not mark Complete**): **Multi-experiment remaining: `WorkSpace` only**.  
Prior session: `sessions/2026.09.10-1047/` (Cycle 11 — continue-strategy re-attest; no material delta; clearance still **NO**; Appendix A unused; zero corpus moves).  
**Do not** reopen Medium wrappers or Early/simple; **do not** re-propose archived Multi-experiment peers (`PWAExemple`, `GitTest`, `WorkStationPWA`) as move sources.  
**Pack type:** short **post–hazard-strategy WorkSpace-only Continuity pack** — **not** Medium soft-deferred finalize-by-default; **not** Cycle 9 pure research+defer default; **not** invent whole-tree archive.

## User intent (raw)

Raw goal: `/full-agent-workflow do next reorganisation cycle` + honor prior SESSION Next FAW lock hint + ROADMAP lock rules. Prior: `sessions/2026.09.10-1047`.  
No explicit ask to **execute Appendix A** / move `_backups`/`_quarantine` → quarantine isolation.  
→ Disclosed Continuity default **A** (continue strategy from durable artifact) applies via `Choose (unanswered→default)`.

## Strategy artifact (exists — point all phases here)

- `program/git-strategy-workspace-hazards.md` (Cycle 10; Cycles 11–12 continue strategy)  
- Classified ≠ cleared for whole-tree; Appendix A isolation unused; whole-tree still fail-closed.

## Plain-language meaning of the gates (carry — brief)

| Term | Everyday meaning |
| --- | --- |
| **Plan gate** | After planning, you see a concrete **from → to** list (if any) and say yes **before** any move. Continuity / “next cycle” is **not** that yes. |
| **fs_mutation** | A plan that moves/renames/deletes real corpus folders on disk (after plan gate). |
| **docs_only** | Docs / INDEX / ROADMAP / session notes / strategy refine only — **zero** corpus folder moves. |
| **Fail-closed** | If a step is still unsafe or unclear, we **stop** instead of guessing. |
| **Atomic nested git** | Treat each nested `.git` tree as one piece — move intact or not at all; no history rewrite. |
| **Continuity** | Reuse Cycles 2–11 rules unless changed below. |
| **SSH-origin path-only** | Relocate/document path as-is; **do not** rewrite `origin` URLs unless a gated git-strategy step says so. |
| **Continue strategy** | Re-probe / refine `git-strategy-workspace-hazards.md` + INDEX honesty from that artifact — still `docs_only`; **not** Cycle 9 pure defer redo; **not** whole-tree archive. |
| **Appendix A execute** | Explicit Continuity to run scoped `_backups`/`_quarantine` isolation — still needs a **separate plan gate**; Continuity Choose ≠ execute. |
| **Taxonomy proposed-ratified** | Ready for user sign-off — **not** final without explicit approval. |
| **Must-preserve draft** | **draft — not auto-locked / for user review**. |

## Clarifying questions (SHORT post–strategy pack — locked without blocking)

Each question framed with plain-language explanation + what yes/each choice commits to + pros/cons.  
**Informed consent:** locking Continuity commits only to path for research/plan — **not** to executing moves (**plan gate still mandatory and separate** if any `fs_mutation` is proposed).  
**Jargon explanation:** recorded here; no debt — pack explained in notes even though user was not re-blocked for a chat round-trip (orchestrator handoff authorized unanswered→default when default is reversible `docs_only`).

### Q1 — Path for `WorkSpace` this cycle? (post–strategy)

**Ask summary:** Continue strategy from durable artifact (default A) vs explicit Appendix A scoped isolation Continuity (B) vs pure research+defer no-op (C)?

**Plain-language:** Strategy docs already exist. What should this cycle do with `WorkSpace`?

| Option | Commits to | Pros | Cons |
| --- | --- | --- | --- |
| **A (default)** | Continue strategy: re-probe live state against `program/git-strategy-workspace-hazards.md`; refine docs / INDEX honesty as needed; **`docs_only`**; classified ≠ cleared; **no** whole-tree archive | Builds on Cycles 10–11; reversible; matches “next” without naming execute | Does not physically isolate `_backups`/`_quarantine` this cycle |
| **B** | Explicit Continuity to **prepare/execute Appendix A** scoped isolation — planner may draft `fs_mutation` map; **still waits for separate plan gate**; Continuity ≠ execute | Progresses physical hazard isolation if you want it | Irreversible FS risk if gate approved; more user attention at plan gate |
| **C** | Pure research+defer honesty pass (minimal / no-op leaning) | Lowest churn | Weak after strategy docs exist; re-defaults toward Cycle 9 style |

**Disclosed default:** **A**. User said “next reorganisation cycle” without naming Appendix A execute → **A**.

### Q2 — Confirm Continuity still applies? (carry; not re-litigated)

**Ask summary:** Carry layout / atomic nested-git / SSH path-only / opaque .env / taxonomy+must-preserve / 90-day / Windows lock recovery / INDEX honesty / never force-move whole tree / never Complete while WorkSpace remains / no peer reopen / agent-owned (no user PATH chores)? Default **A**.

| Option | Commits to | Pros | Cons |
| --- | --- | --- | --- |
| **A (default)** | Carry Continuity as locked below | Stable multi-cycle rules | Does not reopen settled gates |
| **B** | User wants to change Continuity (must name what) | Flexibility | Requires explicit new answers |

**Disclosed default:** **A**. Unanswered → **A**.

### Q3 — Taxonomy / must-preserve pending gates?

**Ask summary:** Do **not** re-block this cycle solely to re-litigate taxonomy final sign-off or must-preserve draft (Continuity may already waive for later move rows). Keep wording **proposed-ratified / draft**. Default **A** = waive re-litigation; proceed with strategy continue.

| Option | Commits to | Pros | Cons |
| --- | --- | --- | --- |
| **A (default)** | Proceed without forcing taxonomy/must-preserve sign-off this cycle | Unblocks strategy continue | Sign-off still pending for later move rows |
| **B** | Pause to pursue taxonomy/must-preserve sign-off now | Clears pending gates | Blocks WorkSpace strategy continue |

**Disclosed default:** **A**. Unanswered → **A**.

---

## Answers

User reply (raw): **`/full-agent-workflow do next reorganisation cycle`** (+ honor prior Next FAW lock hint / ROADMAP; prior `sessions/2026.09.10-1047`). No separate Continuity letter reply; no Appendix A execute named.  
Interpreted as accepting disclosed Continuity defaults for the post–strategy short pack. **Plan gate still mandatory** before any moves — Continuity / “next” **≠** approve a from→to map.

| Q | Ask summary | Locked decision | Source | Rationale |
| --- | --- | --- | --- | --- |
| 1 | Continue strategy (A) vs Appendix A Continuity (B) vs pure defer (C)? | **A** — **Continue strategy** from `program/git-strategy-workspace-hazards.md`: re-probe / refine docs / INDEX honesty; **`docs_only`**; classified ≠ cleared; **no** whole-tree archive. Appendix A **not** Continuity-selected. | `Choose (unanswered→default)` | “Next” after strategy docs → A per FAW law; default is reversible `docs_only` (no re-ask required) |
| 2 | Carry Continuity? | **A** — Carry Continuity as locked below | `Choose (unanswered→default)` | Do not re-litigate Cycles 2–11 Continuity |
| 3 | Waive taxonomy/must-preserve re-block? | **A** — Do not re-block solely to re-litigate; keep proposed-ratified / draft wording | `Choose (unanswered→default)` | Orchestrator pending-gates note |

**Informed consent note:** Q1–Q3 option tables + explanations recorded in this file before locking unanswered→default. Default path is **docs_only** (reversible) — irreversible `fs_mutation` was **not** the default, so no blocking re-ask. Bare Continuity / next **≠** plan-gate move approval.

## Continuity (locked — do not re-ask unless user changes)

| Lock | Value |
| --- | --- |
| ROADMAP row | **Multi-experiment remaining `WorkSpace` only** (orchestrator-locked) — same row; **not** Primary next; **not** Complete while it remains |
| Remaining candidate only | `WorkSpace` |
| Already moved (do not re-propose) | `GitTest`, `WorkStationPWA` (Cycle 7); `PWAExemple` (Cycle 8) → archive |
| Prior rows | Medium wrappers **Complete**; Early/simple **Complete** — do not reopen |
| Path (Q1=A) | **Continue strategy** from durable artifact — **not** Cycle 9 pure-defer default; **not** Appendix A execute Continuity |
| Mutation class | **`docs_only`** this cycle (continue strategy). Appendix A scoped `fs_mutation` only if a **future** cycle locks Q1=B **and** separate plan gate approves a concrete map. Whole XL tree archive under ordinary Multi-experiment Continuity = **OUT OF SCOPE** |
| Strategy pointer | `program/git-strategy-workspace-hazards.md` — primary Context for research/plan/implement |
| Layout | archive / paused / active per research status (catalogue taxonomy) |
| Nested git | Atomic intact `.git` trees; fail-closed on worktrees / multi-remote / unexpected roots unless a gated remediation plan explicitly addresses them |
| Secrets | Opaque `.env` — path presence only; never read/quote |
| Remotes | SSH/origin **path-only** (no rewrite) unless a later gated strategy step says otherwise |
| Status heuristic | 90-day Continuity (research confirms from disk if classifying) |
| Windows lock recovery | Prefer single move; on PermissionDenied/split → reunify + `robocopy /E /MOVE`; empty `.git` shells only; re-attest; log deviation (only if a future approved scoped move runs) |
| Taxonomy | **proposed-ratified — ready for user sign-off** (not final without explicit approval) |
| Must-preserve | **draft — not auto-locked / for user review** |
| INDEX honesty | Document strategy continue progress; do not claim moves that did not happen; do not claim hazards “cleared” if only re-documented |
| Pending hard blockers | None; do not re-block solely to re-litigate taxonomy/must-preserve |
| Agent-owned | Do not dump “user must fix remotes/PATH” as Continuity chores |

## Assumptions

- “Next reorganisation cycle” + prior Next FAW lock hint = Continuity Q1=A continue strategy (`docs_only`).
- Strategy artifact exists → do **not** re-default to Cycle 9 research+defer-as-primary narrative.
- Classified ≠ cleared; whole-tree still fail-closed; Cycle 11 clearance still **NO** until research re-probes.
- Continuity / unanswered→default ≠ move approval; plan gate remains mandatory if any corpus move map is ever proposed.
- Research may re-probe against the durable artifact; focus on refine/honesty, not inventing substitute move sources.
- If hazards still unsafe for whole-tree → process pass with docs + honest INDEX/ROADMAP; row stays in progress (valid).
- Appendix A remains available for a **later** Continuity that explicitly selects B + plan gate — not this cycle’s locked path.

## Open risks

- Treating “next” as Appendix A execute approval would be a process fail.
- Treating Continuity lock as move approval would be a process fail.
- Marking Multi-experiment Complete while `WorkSpace` remains would be a process fail.
- Silent whole-tree archive would be a process fail.
- Re-defaulting to Cycle 9 pure defer while ignoring the durable strategy artifact would be alignment debt.
- Claiming hazards cleared without evidence would corrupt INDEX honesty.

## Self-improvement backlog (if any)

- None for this phase (informed-consent tables recorded; reversible default locked without chat round-trip per orchestrator handoff).

## Key alignment changes (raw → refined)

- Raw: `/full-agent-workflow do next reorganisation cycle` + prior SESSION Next FAW lock hint (`sessions/2026.09.10-1047`)
- Refined: Cycle 12 Multi-experiment **`WorkSpace` only**; Q1=A **continue strategy** from `program/git-strategy-workspace-hazards.md` (`docs_only`); Q2=A Continuity carry; Q3=A no taxonomy re-block; **not** Appendix A execute; **not** Cycle 9 pure defer; never Complete / Primary next / whole-tree archive while WorkSpace remains; no Medium/archived peer reopen.

## Status

**`complete`** — refined prompt ready for researcher handoff.  
**`unanswered_questions`:** none (defaults locked).

## One-line Continuity pack summary (user relay)

Cycle 12 Continuity locked Q1–Q3=A (continue strategy docs_only from hazard artifact; carry rules; waive taxonomy re-block) — say Appendix A / Continuity B next time if you want scoped `_backups`/`_quarantine` isolation with a separate plan gate.
