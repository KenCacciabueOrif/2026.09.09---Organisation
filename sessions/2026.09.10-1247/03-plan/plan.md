# Plan — Cycle 12 Multi-experiment (`WorkSpace` only / continue strategy)

**Session:** `sessions/2026.09.10-1247/03-plan/`  
**Inputs:** `01-prompt-betterment/refined-prompt.md`, `01-prompt-betterment/notes.md`, `02-research/research-brief.md` (+ findings)  
**Approach:** Research **Option A** (Continuity **Q1=A**) — **continue strategy** from durable artifact `program/git-strategy-workspace-hazards.md`; re-attest live re-probe (**no material delta** vs Cycle 11 / Cycle 10); refresh INDEX / ROADMAP / optional inventory honesty. **Mutation class `docs_only`.** Zero corpus moves. **No** Appendix A execute. **Not** Cycle 9 pure research+defer theater. Multi-experiment stays **in progress** with **Remaining: `WorkSpace` only**.

---

## Goal

Run one FAW program cycle for the orchestrator-locked ROADMAP row **Multi-experiment remaining: `WorkSpace` only**, under **post–hazard-strategy Continuity Q1=A**: re-probe live `C:\Project\WorkSpace` against the durable classification in `program/git-strategy-workspace-hazards.md`, record a Cycle 12 attestation that hazards show **no material delta** vs Cycle 11 and remain **classified ≠ cleared**, and update strategy / INDEX / ROADMAP honesty accordingly — all **`docs_only`**, with **zero** intentional corpus path moves/renames/deletes. Do **not** execute Appendix A `_backups`/`_quarantine` isolation; do **not** whole-tree archive; do **not** mark Multi-experiment Complete or jump Primary next / Special git while `WorkSpace` remains; do **not** reopen Medium / Early or archived Multi-experiment peers as move sources.

---

## Mutation class

| Field | Value |
| --- | --- |
| Class | **`docs_only`** |
| Corpus FS | **Zero** intentional corpus moves/renames/deletes — do not touch `C:\Project\WorkSpace`, `_backups`, `_quarantine`, nested roots, or any other catalogue-backed folder path |
| Org-repo disk writes | Allowed: refine `program/git-strategy-workspace-hazards.md`; honesty edits to `program/ROADMAP.md`, `catalogue/INDEX.md`, optional `catalogue/inventory.md`; session logs under `sessions/2026.09.10-1247/` — all inside this organisation git root only |
| User approval before implementer | **Not required** (`docs_only`) — **no mandatory corpus plan-gate pause** |
| First-move / Early-simple gates | **N/A** — no executed `fs_mutation`; Continuity Q3=A — do not re-block solely on taxonomy/must-preserve re-litigation |
| Push / remote publish | **Not required** — dual auth/preflight **N/A** |

### Orchestrator note (plan gate)

**No mandatory plan-gate pause** before implementer. Continuity Q1=A already locked **continue strategy** (`docs_only`); there is **no** corpus path map to approve this cycle. Proceed to implementer on this plan as written.

**Continuity ≠ move approval.** Continuity / Choose / “next reorganisation cycle” / unanswered→defaults authorize this **docs_only** path only. They do **not** authorize Appendix A execute, whole-tree archive, or any corpus `fs_mutation`.

If a **future** cycle wants to execute Appendix A (scoped `_backups` / `_quarantine` isolation), that cycle must lock Continuity **Q1=B**, set `mutation_class: fs_mutation`, include a full “What the user is approving” section with a concrete from→to map, and wait for a **separate** plan-gate yes — Continuity / Choose / “next” from this cycle does **not** authorize that execute.

### Implementer attestation (zero corpus FS)

After docs steps, implementer must attest in the implementation log:

1. Presence of `C:\Project\WorkSpace` at root (unchanged location).
2. Presence of `…\TestNewWorkspaceAgent\_backups` and `…\_quarantine` still under WorkSpace (unchanged location).
3. No `Move-Item` / `robocopy` / rename targeting WorkSpace or any other corpus folder this cycle.
4. Dated whole-tree destinations `C:\Project\archive\2026.05.29 - WorkSpace` and paused/active equivalents remain **absent**.
5. No `.env` contents read or logged (opaque path presence / counts OK only).
6. No `git remote set-url` / remote rewrite / history rewrite on any WorkSpace nested root.
7. Appendix A **not executed**.

**Probe method (required in log):** Prefer `Test-Path` when Shell works. If Shell stdout is empty / unavailable, **`Read`** and/or **`Glob`** are acceptable equivalents (directory-present / File-not-found semantics as in research). Log **which probe method** was used for each attestation — do **not** fail AC solely because Shell was unavailable.

---

## What the user is approving

**N/A — docs_only continue-strategy attestation.** There is no corpus move map and no separate plan-gate ask this cycle.

- **Continuity lock** (already applied via unanswered→default) = path for research/plan/implement: continue strategy docs from the durable artifact.
- **Continuity ≠ move approval** — locking Q1=A does **not** approve any from→to map; this cycle has **zero** corpus moves by design.
- Material user-facing changes are **org-repo documentation only** (Cycle 12 re-probe attestation on the durable strategy artifact + INDEX/ROADMAP honesty). They do **not** relocate files on disk and do **not** claim Multi-experiment Complete, Primary-next jump, or hazards “cleared.”

---

## Explicit move map (this cycle)

**None.** Empty by design (`docs_only` / Continuity Q1=A).

| Folder | Disposition this cycle |
| --- | --- |
| `C:\Project\WorkSpace` (whole tree) | **Remain at root** — document only; whole-tree archive OOS |
| `…\TestNewWorkspaceAgent\_backups` | **Remain** — Appendix A unused; isolation deferred |
| `…\TestNewWorkspaceAgent\_quarantine` | **Remain** — Appendix A unused; isolation deferred |
| Live primary nested roots `#1–#5` | **Keep in place** — multi-remote still classified; no remote surgery |
| Backup/quarantine hermes clones `#6–#7` | **Keep in place** — document-only |
| `PWAExemple`, `GitTest`, `WorkStationPWA` | Verify-only at archive paths — **never re-propose** as sources |
| Medium / Early archive & paused | Verify-only — **never reopen** as sources |

---

## Context (cite — do not rewrite strategy from scratch)

- **Primary strategy artifact:** `program/git-strategy-workspace-hazards.md` (Cycle 10 + Cycle 11 attestation) — refine with Cycle 12 re-probe attestation; keep classification / fate / safe rules as authoritative baseline.
- Research: live re-probe = **no material delta** vs Cycle 11 / Cycle 10 (nested **7**; multi-remote `origin`+`cada` on 3 hermes trees; live HEAD `4fbff573…` ≠ clone `8d60d929…`; linked worktrees none; opaque `.env*` still present; XL band carried ~5786 MB). Probe method this research turn: **`Read` / `Glob`** (Shell stdout empty).
- **Clearance for whole-tree:** still **NO**.
- Continuity locks: Q1=A continue strategy; Q2=A carry; Q3=A no taxonomy/must-preserve re-block.
- Taxonomy: **proposed-ratified — ready for user sign-off** (not final). Must-preserve: **draft — not auto-locked / for user review**.

---

## Acceptance criteria

- [ ] Research / plan / implement **anchor on** `program/git-strategy-workspace-hazards.md`; cover **`WorkSpace` only**; archived peers verified not re-proposed as sources.
- [ ] Cycle declared **`docs_only`**; implementer log attests **zero** corpus path moves/renames/deletes (no Appendix A execute).
- [ ] Strategy artifact updated with Cycle 12 re-probe attestation (date/session; **no material delta** vs Cycle 11; clearance still **NO**; Appendix A still unused; classified ≠ cleared).
- [ ] `catalogue/INDEX.md` and `program/ROADMAP.md` honesty updated for Cycle 12 continue-strategy; do **not** claim hazards cleared if only re-documented; do **not** claim moves that did not happen.
- [ ] Multi-experiment row stays **in progress** with **Remaining: `WorkSpace` only** — **never** Complete / Primary next / Special git jump while it remains.
- [ ] **No whole-tree archive / force-move** of `WorkSpace` under ordinary Multi-experiment Continuity.
- [ ] Medium / Early / archived Multi-experiment peers **not** reopened as move sources.
- [ ] Opaque `.env`, atomic nested git, SSH/HTTPS path-only Continuity respected (document-only for remotes this cycle).
- [ ] Taxonomy / must-preserve language stays **proposed-ratified — ready for user sign-off** / **draft — not auto-locked / for user review** — not claimed final user ratification; no sole re-block on those gates.
- [ ] No remote URL rewrite / history rewrite / `.env` content reads; no agent `git push` / `git pull` required.
- [ ] Zero-move path attestation logged with **probe method** named (`Test-Path` **or** `Read`/`Glob` equivalent when Shell unavailable).
- [ ] Next-FAW lock hint (ROADMAP / implementation close notes) points to **continue strategy** from the durable artifact **or** explicit Continuity **B** + plan gate for Appendix A — **not** soft-deferred force-finalize / Complete / Primary next while WorkSpace remains.
- [ ] Auditor can verify all AC from session artifacts without chat history.

**Not hard AC (include when cheap):** absolute size MB re-measure; optional `catalogue/inventory.md` Cycle 12 honesty line.

---

## Steps

1. **Confirm research lock (read-only)** — paths: `02-research/research-brief.md`, `02-research/codebase-findings.md`, Continuity in `01-prompt-betterment/notes.md` / refined-prompt — action: restate Option A / Q1=A continue strategy / empty execute map / “not Cycle 9 pure defer” in implementation log header — verify: log cites mutation_class **`docs_only`**; hazards cleared for whole-tree = **NO**; Appendix A marked non-executed; Continuity Q1≠B.

2. **Pre-flight presence (attest, do not move)** — paths: `C:\Project\WorkSpace`; `C:\Project\WorkSpace\TestNewWorkspaceAgent\_backups`; `C:\Project\WorkSpace\TestNewWorkspaceAgent\_quarantine`; archive peers `C:\Project\archive\2025.08.08 - GitTest`, `…\2025.07.01 - WorkStationPWA`, `…\2025.06.25 - PWAExemple` — action: `Test-Path` **or** `Read`/`Glob` equivalent — verify: WorkSpace + `_backups` + `_quarantine` **present** at current locations; peers **present** at archive; WorkSpace dated archive/paused targets **absent**; **log probe method**.

3. **Refine durable strategy artifact (continue — do not rewrite from scratch)** — path: **`program/git-strategy-workspace-hazards.md`** — action: add a **Cycle 12 re-probe attestation** section (or equivalent date stamp) that records:
   - Session pointer: `sessions/2026.09.10-1247/`
   - Continuity Q1=A continue strategy (`docs_only`)
   - Live result: **no material delta** vs Cycle 11 attestation / Cycle 10 classification (nested **7**; multi-remote live+clones; HEAD divergence live vs backup/quarantine; linked worktrees none; opaque `.env*` still present; XL band carried)
   - **Clearance for whole-tree archive: still NO** — classified ≠ cleared
   - Appendix A still **unused** / not Continuity-selected this cycle
   - Preserve existing hazard tables, fate, safe rules, and prior Cycle 11 attestation (edit only for honesty deltas if any; do not invent substitute clearance)
   - Taxonomy / must-preserve wording unchanged: **proposed-ratified — ready for user sign-off** / **draft — not auto-locked / for user review**
   - Update header session pointers / Next FAW lock hint as warranted
   — verify: file still primary Continuity baseline; no from→to execute instructions implying moves now; no Complete / Primary-next language; no claim hazards “cleared.”

4. **Update ROADMAP Notes** — path: `program/ROADMAP.md` (Multi-experiment row Notes + “How the next cycle starts” § for Multi-experiment) — action: append Cycle 12 session `2026.09.10-1247` **docs_only continue-strategy** (re-probe attestation; pointer to `program/git-strategy-workspace-hazards.md`); keep **In progress (partial / nearly complete)**; **Remaining: `WorkSpace` only**; still fail-closed for whole-tree; **not** Complete — verify: no “Complete”; no Primary-next / Special git jump while WorkSpace remains; Next lock hint = continue strategy **or** explicit Continuity B + plan gate for Appendix A (not soft-deferred force-finalize; not Cycle 9 pure-defer-as-primary).

5. **Update INDEX honesty** — path: `catalogue/INDEX.md` (WorkSpace table row + “What’s next” Multi-experiment item) — action: keep path `C:\Project\WorkSpace`; note Cycle 12 continue-strategy pass + link to strategy artifact; hazards still classified not cleared; remaining = WorkSpace only; row not Complete — verify: no current-path claiming `archive\2026.05.29 - WorkSpace`; taxonomy/must-preserve not upgraded to final.

6. **Optional inventory honesty** — path: `catalogue/inventory.md` (WorkSpace row / related nested notes) — action: when cheap, add Cycle 12 continue-strategy line; retain nested **7** honesty; state still at root / not moved — verify: no false “moved Cycle 12”; not a hard AC if skipped with log note.

7. **Session implementation artifacts** — paths: `sessions/2026.09.10-1247/04-implementation/log.md`, `changes.md` — action: record docs_only steps, zero-move attestation (incl. `_backups`/`_quarantine` still under WorkSpace) with **named probe method**, pointer to refined strategy section, explicit “Appendix A not executed”; include **Next FAW lock hint**: continue strategy from `program/git-strategy-workspace-hazards.md` **or** Appendix A only with explicit Continuity B + separate plan gate — verify: explicit “corpus moves: 0”; opaque `.env` unread; Remaining WorkSpace only.

8. **Self-check before handoff** — action: grep/scan ROADMAP + INDEX + strategy artifact for accidental “Complete”, Primary-next jump past Multi-experiment, WorkSpace archive path as **current**, “hazards cleared”, Cycle 9 pure-defer-as-primary narrative, or move commands — verify: remaining WorkSpace only; still in progress; continue-strategy (not defer theater); Appendix A clearly non-executed.

**Skipped (explicit):** Auth/preflight for push/pull — **N/A**. Pull/sync — **N/A**. Any `Move-Item` / robocopy / remote surgery — **out of scope this cycle**. Appendix A execute — **out of Continuity (needs future Q1=B + plan gate)**. Taxonomy/must-preserve sign-off chase — **waived re-block (Q3=A)**. Mandatory user plan gate — **not required** for this `docs_only` cycle.

---

## Appendix A — Deferred scoped isolation (NOT executed; NOT Continuity-selected)

**Status:** Available only for a **future** cycle that locks Continuity **Q1=B** and obtains a **separate plan-gate** yes on an `fs_mutation` map. **Do not implement in Cycle 12.** Continuity Q1=A / Choose / “next” **≠** authorization to run this map.

Illustrative from→to remains documented in Cycle 10 plan Appendix A (`sessions/2026.09.10-0907/03-plan/plan.md`) and referenced from the durable strategy artifact — **re-confirm destinations free** at future plan time. Expect nested-root count **7→5** if both parents move; live hermes multi-remote **remains**; whole-tree archive still **blocked**; Multi-experiment stays **in progress** / Remaining WorkSpace only.

---

## Non-goals

- Executing Appendix A or any corpus move/rename/delete this cycle
- Whole XL `WorkSpace` tree archive / force-move under ordinary Multi-experiment Continuity
- Marking Multi-experiment **Complete** or jumping **Primary next** / Special git while `WorkSpace` remains
- Reopening Medium / Early or re-proposing archived `PWAExemple` / `GitTest` / `WorkStationPWA` as sources
- Rewriting the entire strategy artifact from scratch (continue/refine only)
- Re-defaulting to Cycle 9 pure research+defer as the primary path while ignoring the durable strategy artifact
- Remote URL rewrite, dropping `cada`, history rewrite, force-push, reading `.env` contents
- Taxonomy final sign-off or must-preserve final lock
- Treating Continuity / “next” as plan-gate move approval
- Agent `git push` / `git pull` for this cycle
- Dumping user chores to “fix remotes/PATH” as Continuity requirements
- Requiring Shell-only path probes when `Read`/`Glob` can attest (log method instead)

---

## Risks / rollback

| Risk | Mitigation |
| --- | --- |
| Implementer executes Appendix A despite docs_only / Q1=A | Empty execute map + AC + attestation; auditor fails on any corpus path change |
| Docs framed as hazards “cleared” or Multi-experiment Complete | Steps 3–8 forbid cleared/Complete/Primary-next; AC requires classified ≠ cleared |
| Cycle 9 pure-defer theater (ignore durable artifact) | Step 3 requires strategy file attestation; non-goal lists Cycle 9 re-default |
| Secrets leak into session docs | Opaque path presence only; no content reads |
| Taxonomy / must-preserve overclaimed as final | Exact proposed-ratified / draft wording retained |
| Accidental sibling-tree git ops | Org-repo root only for doc writes |
| Shell unavailable → false AC fail on path probes | AC allows Read/Glob; require probe method in log |

**Rollback:** revert org-repo doc edits via git; no corpus rollback needed if attestation holds (nothing moved).

---

## Ready to implement

**yes**

**Rationale:** Continuity Q1=A / Q2=A / Q3=A locked; research Option A ready with no material delta and clearance still NO; empty execute map; `docs_only` with zero corpus FS mutations; no auth/push blocker; no mandatory plan gate; no blocking product decisions remain.

## Blocking questions

**none**

---

## Plan result (for orchestrator)

- **plan_path:** `sessions/2026.09.10-1247/03-plan/plan.md`
- **ready_to_implement:** yes
- **mutation_class:** `docs_only`
- **plan_gate_needed:** no
- **blocking_questions:** none
- **step_count:** 8
- **one-line summary:** Docs-only Cycle 12: re-attest WorkSpace hazards on durable strategy artifact + INDEX/ROADMAP honesty; zero moves; row stays Remaining WorkSpace only.
