# Refined prompt — Cycle 14

## Goal

**One-liner:** Draft (STAGE 1 only) a Continuity **B** / Appendix A **`fs_mutation`** plan to relocate the intact `_backups` and `_quarantine` parent trees out of the live `C:\Project\WorkSpace` tree per `program/git-strategy-workspace-hazards.md` safe relocation rules — then **stop for the human plan gate** (Hermes); do **not** execute moves in this STAGE 1 run.

Run FAW phases **1–3** (prompt betterment → research → plan) for **Cycle 14 — Multi-experiment (WorkSpace only)**. Planner produces a scoped isolation move map. Implementer is **out of band** until the user approves the plan gate in Hermes chat. If the plan is **not** Continuity B scoped Appendix A isolation, **stop at plan gate** (do not proceed as docs_only re-attest or whole-tree archive).

## Constraints

- **ROADMAP lock:** Multi-experiment remaining **`WorkSpace` only** — **in progress / not Complete**. Never jump Primary next / Special git. Never reopen Medium / Early. Never re-propose archived peers (`PWAExemple`, `GitTest`, `WorkStationPWA`) as move sources.
- **Mutation class:** `fs_mutation` (Continuity **B** locked by user). **Do not** propose `docs_only` re-attest as the cycle path (Cycles 10–13 already re-attested no material delta four times).
- **Scope of moves (when later approved):** **Appendix A only** — whole parent trees `_backups` and `_quarantine` **out of** the live WorkSpace tree. Prefer named parents; do not cherry-pick empty `.git` shells.
- **Atomic intact nested-git moves** — move trees that contain `.git` intact; never strip history, `filter-repo`, force-push, or history rewrite.
- **No remote rewrites** — path-only Continuity; inventory remotes; never `git remote set-url` / origin rewrite unless a later gated step explicitly says so (default: document only).
- **Zero whole-tree WorkSpace archive/move** — live primary roots stay in place; classified ≠ cleared for whole-tree fate.
- **Opaque `.env*`** — path presence only; never read/quote/log contents; list unread paths in plan intent-preview.
- **Windows nested-`.git` lock recovery** — follow strategy artifact (single move preferred; PermissionDenied → reunify + `robocopy /E /MOVE`; remove only empty leftover `.git` shells; re-attest; log deviation).
- **Linked worktrees:** re-probe `worktree list` before any future approved move; if unexpected >1, fail-closed.
- **Taxonomy:** **proposed-ratified — ready for user sign-off** (not final). **Must-preserve:** **draft — not auto-locked / for user review**. Q3=A — waive re-litigation this cycle.
- **Consent:** Continuity / Choose ≠ plan-gate approval. STAGE 1 holds the plan gate in Hermes; this cycle’s Continuity B only authorizes drafting the map.
- **Agent-owned:** no dumping PATH/remote fix chores on the user as Continuity requirements.
- **Org-repo hygiene:** session artifacts under `sessions/2026.09.11/`; do not mutate corpus until post-gate implementer.

## Context pointers

- Strategy (authoritative): `program/git-strategy-workspace-hazards.md` — hazard tables, recommended fate, **safe relocation / isolation rules**, Appendix A guidance, Next FAW lock hint.
- ROADMAP: `program/ROADMAP.md` (Multi-experiment / WorkSpace only — in progress).
- Prior session (Cycle 13 docs_only): `sessions/2026.09.10-1630/`.
- Illustrative (non-executed) Appendix A draft map: `sessions/2026.09.10-0907/03-plan/plan.md` — destinations must be **re-confirmed free**.
- Catalogue: `catalogue/INDEX.md`, `catalogue/inventory.md`, `catalogue/must-preserve.md`.
- Live candidate root: `C:\Project\WorkSpace` (still at INDEX root until a future gated whole-tree clearance — **not this cycle**).

## Acceptance criteria

- [ ] Researcher grounds findings in live re-probe **and** `program/git-strategy-workspace-hazards.md` (Appendix A / safe relocation); does **not** default the cycle to docs_only re-attest theater.
- [ ] Planner `mutation_class: fs_mutation` with Continuity **B** / Appendix A scope only (`_backups` + `_quarantine` parent trees out of live WorkSpace).
- [ ] Plan includes full **“What the user is approving”** intent-preview (from→to, size bands, nested-git count expectation e.g. 7→5 if both move, opaque `.env*` path list unread, remotes unchanged).
- [ ] Plan encodes: atomic intact nested-git; **no** remote rewrite; **no** history rewrite; **no** whole-tree WorkSpace archive; live primary roots keep-in-place; Multi-experiment stays **in progress** / Remaining WorkSpace only after isolation.
- [ ] Plan cites safe relocation rules 1–10 from the strategy artifact; Windows lock-recovery path documented.
- [ ] Destinations re-validated free (Cycle 10 illustrative paths are hints only).
- [ ] `ready_to_implement` may be `yes` for planner completeness, but orchestrator **STAGE 1** does **not** launch implementer; plan gate remains **human-held in Hermes**.
- [ ] If plan drifts off Continuity B scoped isolation → **stop at plan gate**; do not auto-continue as docs_only or whole-tree.
- [ ] Taxonomy/must-preserve language remains **proposed-ratified** / **draft** (not claimed final); Q3 waiver documented in session notes.
- [ ] Zero corpus FS mutation executed in STAGE 1 (phases 1–3 only).

## Out of scope

- Implementer / auditor / corpus moves in this STAGE 1 run.
- Docs_only Continuity A re-attest as the chosen cycle path.
- Cycle 9-style pure research+defer theater.
- Whole-tree archive or move of `C:\Project\WorkSpace`.
- Moving or “fixing” live primary nested roots `#1–#5` under WorkSpace.
- Reopening Medium / Early or archived Multi-experiment peers as sources.
- Marking Multi-experiment **Complete** or jumping Primary next / Special git.
- Remote URL rewrite, history rewrite, force-push, `filter-repo`, deleting backup/quarantine by default.
- Publish/pull of org-repo (unless a later explicit cycle asks).
- Clearing live hermes multi-remote hazard (isolation does not clear it).
- User PATH / credential chores framed as Continuity requirements.
