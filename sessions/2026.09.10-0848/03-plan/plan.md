# Plan — Cycle 9 Multi-experiment (`WorkSpace` only, docs_only defer)

**Session:** `sessions/2026.09.10-0848/03-plan/`  
**Inputs:** `01-prompt-betterment/refined-prompt.md`, `02-research/research-brief.md` (+ findings)  
**Approach:** Research **Option A** — **fail-closed defer** `WorkSpace`. **No green from→to move map.** Mutation class **`docs_only`**: session artifacts + honest INDEX / inventory / ROADMAP Notes that Cycle 9 re-probed hazards and deferred; Multi-experiment stays **in progress** with **Remaining: `WorkSpace` only**. Do **not** mark Complete; do **not** jump Primary next / Special git; do **not** invent git-strategy or force-move.

---

## Goal

Close the Continuity **Q1=A** research branch for the orchestrator-locked ROADMAP row **Multi-experiment / `WorkSpace` only**: record that live hazards on `C:\Project\WorkSpace` are **not clearly cleared** (messy multi-remote, unexpected `_backups`/`_quarantine` nested roots, inventory undercount 3→**7**, XL, must-preserve draft Medium caution), keep the tree at its INDEX root path with **zero** intentional corpus moves, and refresh org-repo honesty docs so auditors see Cycle 9 researched+deferred — row still open, remaining `WorkSpace` only. No dedicated git-strategy body this cycle (Q1≠B). No agent push/pull.

---

## Mutation class

| Field | Value |
| --- | --- |
| Class | **`docs_only`** |
| Corpus FS | **Zero** intentional corpus moves/renames/deletes — do not touch `C:\Project\WorkSpace` or any other catalogue-backed folder path |
| Org-repo disk writes | Allowed: session logs under `sessions/2026.09.10-0848/`; honesty edits to `program/ROADMAP.md`, `catalogue/INDEX.md`, and `catalogue/inventory.md` (nested-count / Cycle 9 defer note) inside this organisation git root only |
| User approval before implementer | **Not required** (`docs_only`) — see orchestrator note below |
| First-move / Early-simple gates | **N/A** — no `fs_mutation`; no move map |
| Push / remote publish | **Not required** — dual auth/preflight **N/A** |

### Orchestrator note (plan gate)

**No mandatory plan-gate pause** before implementer for this cycle. Continuity / Choose “yes” already locked research+defer; there is **no** path map to approve. Proceed to implementer on this plan as written. If a future cycle proposes a green map or git-strategy, that cycle must use `fs_mutation` + mandatory plan gate.

### Implementer attestation (zero corpus FS)

After docs steps, implementer must attest in the implementation log:

1. `Test-Path C:\Project\WorkSpace` → still **True** at root (unchanged location).
2. No `Move-Item` / `robocopy` / rename targeting `WorkSpace` or any other corpus folder this cycle.
3. Dated destinations `C:\Project\archive\2026.05.29 - WorkSpace` and paused/active equivalents remain **absent** (no accidental move).
4. No `.env` contents read or logged (opaque path mentions in docs OK only as counts / relative paths already in research).

---

## What the user is approving

**N/A — docs_only defer attestation.** There is no corpus move map and no separate plan-gate ask.

Material user-facing doc changes this cycle are **honesty updates only** (ROADMAP Notes + INDEX/inventory reflecting Cycle 9 researched+deferred hazards). They do **not** relocate files on disk and do **not** claim Multi-experiment Complete or jump Primary next. Continuity Q1=A already authorized research-then-defer; implementer may apply the doc edits without a further yes/no gate.

Hypothetical future destination (documented for honesty only — **do not schedule**):  
`C:\Project\WorkSpace` → `C:\Project\archive\2026.05.29 - WorkSpace`.

---

## Explicit move map

**None.** Empty by design (fail-closed).

| Folder | Disposition this cycle |
| --- | --- |
| `C:\Project\WorkSpace` | **Fail-closed defer** — remain at root; docs only |
| `PWAExemple`, `GitTest`, `WorkStationPWA` | Verify-only at Cycle 7/8 archive paths — **never re-propose** as sources |
| Medium / Early archive & paused | Verify-only — **never reopen** as sources |

---

## Acceptance criteria

- [ ] Hazard outcome documented as **fail-closed defer** with concrete reasons (multi-remote; `_backups`/`_quarantine` extra roots; **7** nested roots vs inventory undercount **3**; XL; must-preserve draft Medium caution — not sole gate).
- [ ] Mutation class is **`docs_only`**; implementer log attests **zero** intentional corpus path moves (including no `WorkSpace` move).
- [ ] `C:\Project\WorkSpace` still exists at INDEX root path after implementation; archive/paused dated targets for WorkSpace remain absent.
- [ ] `program/ROADMAP.md` Multi-experiment Notes updated for **Cycle 9** (`sessions/2026.09.10-0848/`): researched+deferred `WorkSpace` + short hazard summary; status remains **In progress**; **Remaining: `WorkSpace` only**; **not** Complete; **Primary next** does **not** jump to Special git / git-strategy as “done with Multi-experiment”.
- [ ] `catalogue/INDEX.md` WorkSpace row stays at `C:\Project\WorkSpace` with honesty note for Cycle 9 defer + hazard/nested-count correction as needed; “What’s next” still shows Multi-experiment remaining `WorkSpace` only (row not Complete).
- [ ] `catalogue/inventory.md` WorkSpace nested-count honesty corrected when editing (**3 → 7** live probe) and/or Cycle 9 defer note; no invented re-moves.
- [ ] No dedicated **git-strategy** plan body or execution this cycle (Q1≠B).
- [ ] Medium / Early / archived Multi-experiment peers **not** reopened as move sources.
- [ ] Taxonomy language remains **proposed-ratified — ready for user sign-off** (not final); must-preserve remains **draft — not auto-locked / for user review**.
- [ ] No agent `git push` / `git pull` required; no secrets in session logs.
- [ ] Auditor can verify all AC from session artifacts without chat history.

---

## Steps

1. **Confirm research lock (read-only)** — paths: `02-research/research-brief.md`, Continuity in `01-prompt-betterment/notes.md` — action: restate Option A / empty map / Q1=A in implementation log header — verify: log cites hazards cleared = **NO**; no move map section with from→to rows.

2. **Pre-flight presence (attest, do not move)** — paths: `C:\Project\WorkSpace`; archive peers `C:\Project\archive\2025.08.08 - GitTest`, `…\2025.07.01 - WorkStationPWA`, `…\2025.06.25 - PWAExemple` — action: `Test-Path` only — verify: WorkSpace **True** at root; peers **True** at archive; WorkSpace dated archive/paused targets **False**.

3. **Update ROADMAP Notes** — path: `program/ROADMAP.md` (Multi-experiment row Notes + “How the next cycle starts” if needed for Cycle 9 pointer) — action: append Cycle 9 session `2026.09.10-0848` researched+deferred `WorkSpace` with brief hazard summary (multi-remote + backup/quarantine roots + nested-count 7 + XL); keep **In progress (partial / nearly complete)**; **Remaining: `WorkSpace` only**; fail-closed / git-strategy **later opt-in**; **not** Complete — verify: no “Complete”; no Primary-next jump that drops Multi-experiment while WorkSpace remains; no soft-deferred-finalize language that implies force-move next.

4. **Update INDEX honesty** — path: `catalogue/INDEX.md` (WorkSpace table row + “What’s next” item 4) — action: keep path `C:\Project\WorkSpace`; note Cycle 9 fail-closed defer + hazard/nested honesty (e.g. live **7** nested / multi-remote / XL); remaining Multi-experiment = WorkSpace only; row not Complete — verify: no destination claiming `archive\2026.05.29 - WorkSpace`; taxonomy/must-preserve wording not upgraded to final.

5. **Update inventory nested-count / defer note** — path: `catalogue/inventory.md` (WorkSpace row / related notes) — action: correct multi-nested undercount **3 → 7** (or equivalent live-probe honesty) and note Cycle 9 docs_only defer; do not invent moves — verify: WorkSpace still listed at root / Multi-experiment remaining; no false “moved Cycle 9”.

6. **Session implementation artifacts** — paths: `sessions/2026.09.10-0848/04-implementation/log.md`, `changes.md` — action: record docs_only steps, zero-move attestation, hazard summary pointer to research-brief — verify: explicit “corpus moves: 0”; opaque `.env` unread.

7. **Self-check before handoff** — action: grep/scan ROADMAP + INDEX for accidental “Complete”, Primary-next jump past Multi-experiment, or WorkSpace archive path as current — verify: remaining WorkSpace only; still in progress.

**Skipped (explicit):** Auth/preflight for push/pull — **N/A**. Pull/sync — **N/A**. Any `Move-Item` / robocopy / git-strategy Steps — **out of scope**.

---

## Non-goals

- Moving, renaming, or copying `WorkSpace` (or any other corpus folder)
- Green from→to map or plan-gate for moves
- Dedicated multi-repo **git-strategy** FAW body or Continuity Q1=B
- Marking Multi-experiment **Complete** or jumping **Primary next** / Special git while `WorkSpace` remains
- Reopening Medium / Early or re-proposing archived `PWAExemple` / `GitTest` / `WorkStationPWA` as sources
- Taxonomy final sign-off or must-preserve final lock
- Reading `.env` contents; rewriting remotes; force-push; history rewrite
- Agent `git push` / `git pull` for this cycle

---

## Risks / rollback

| Risk | Mitigation |
| --- | --- |
| Implementer invents a move map despite docs_only | Empty map + AC + attestation; auditor fails if any corpus path change |
| ROADMAP marked Complete / Primary next jumped | Steps 3–7 forbid; auditor greps Complete / remaining |
| Nested-count honesty left at stale “3” | Step 5 corrects to live **7** when editing inventory |
| Docs imply git-strategy started | Explicit non-goal; Notes say later opt-in only |
| Opaque `.env` read during “honesty” edits | Path/count only; never open files |
| Overclaim taxonomy / must-preserve final | Keep **proposed-ratified — ready for user sign-off** / **draft — not auto-locked** |
| Accidental sibling-tree git ops | Org-repo root only for any later publish; this plan does not require commit/push |

**Rollback:** Revert org-repo doc edits via git if needed. No corpus rollback required when zero moves attested.

---

## Ready to implement

**yes** — docs-only steps are clear; empty move map; no plan-gate required; no push/pull blocker.

## Blocking questions

**none**
