# Refined prompt — Cycle 18

## Goal

Run **STAGE 1 only** (research → plan → **STOP**) for Multi-experiment remaining **`C:\Project\WorkSpace` only**, under Continuity **Q1=B→E** and CaDa’s **anti-loop** rule:

1. **Prefer:** research a **genuinely new narrow gated** `fs_mutation` advance **not already covered/executed** by `program/git-strategy-workspace-hazards.md`.
2. **Else:** if none exists (or path would be **no material delta** vs Cycle 16), go straight to **E** — planner must deliver **break-the-cycle escalate options** (plain language + tradeoffs). **Do not** plan another Continuity A / `docs_only` re-attest theater.

Continuity ≠ plan-gate execute. STAGE 1 stops after planner.

## Constraints

- **ROADMAP lock:** Multi-experiment **Remaining: `WorkSpace` only** — row **in progress / not Complete**. Never mark Complete; never jump Primary next / Special git while WorkSpace remains.
- **Do not** reopen Medium / Early/simple; **do not** re-propose archived peers (`PWAExemple`, `GitTest`, `WorkStationPWA`, …).
- **Do not** soft-deferred force-finalize / force-move the XL WorkSpace tree; **do not** invent whole-tree archive under ordinary Continuity.
- **Appendix A** parents already isolated (Cycle 14) — do not re-propose the same `_backups`/`_quarantine` isolation.
- **Live multi-remote** cleared (Cycle 15; confirmed Cycle 16) — do not re-clear unless regression; regression → fail-closed + concrete unblock, not Complete.
- Point all phases at durable strategy artifact: `program/git-strategy-workspace-hazards.md`.
- Nested `.git` stays **atomic**; remotes **path-only** (no origin URL rewrite unless a later gated step says so); opaque `.env*` unread.
- Taxonomy / must-preserve: **Q2=A** waive re-litigation — taxonomy stays **proposed-ratified — ready for user sign-off**; must-preserve stays **draft — not auto-locked / for user review**.
- Dirty WT hygiene: **Q3=A** defer — disclose only; **NO_AUTO_COMMIT**.
- **STAGE 1 ONLY:** no implementer this run; Continuity / Choose **≠** plan-gate execute.
- **Anti-loop (binding):** no material delta = escalate. Forbidden fallback: Continuity A / `docs_only` re-attest.

## Context pointers

- `program/ROADMAP.md` (Primary next → Multi-experiment; WorkSpace only; Cycle 16 Next FAW lock hint)
- `program/git-strategy-workspace-hazards.md` (authoritative hazard / safe rules / post–14/15/16 attestations)
- Reorg Continuity: `sessions/2026.09.11-1038/` (Cycle 16), `sessions/2026.09.11-0859/` (Cycle 15), `sessions/2026.09.11/` (Cycle 14 Appendix A)
- Named prior `sessions/2026.09.14-1004/` — **unrelated** product_settings complete; **do not resume**
- Catalogue: `catalogue/INDEX.md`, `catalogue/inventory.md`, `catalogue/must-preserve.md`
- This session: `sessions/2026.09.15/` · Continuity lock `01-prompt-betterment/notes.md`

## Acceptance criteria

- [ ] Continuity locked: **Q1=B→E**, **Q2=A**, **Q3=A** (recorded in `notes.md` with raw user reply).
- [ ] Research cites `program/git-strategy-workspace-hazards.md` and Cycles 14–16; verifies live residual state (nested count, multi-remote, XL / whole-tree clearance) without inventing Complete.
- [ ] **Anti-loop gate:** research either (a) names a **genuinely new** narrow gated candidate **not** already covered/executed by the hazard artifact, with explicit “why new” justification, **or** (b) states **no new scoped step** / no-material-delta path → **escalate required**.
- [ ] Plan mutation class is **exactly one** of: `fs_mutation` (narrow map + mandatory separate plan gate later) **or** `escalate_break_loop` (break-the-cycle options). **Never** `docs_only` Continuity A theater under this Continuity.
- [ ] If escalate: plan lists **clear user options** (plain language + tradeoffs) — e.g. dedicated XL/git-strategy Continuity; park WorkSpace with honesty; user-named exception Continuity — **not** “run Continuity A again”.
- [ ] If `fs_mutation` map proposed: named parents/paths only; no whole-tree archive; Continuity ≠ execute; STAGE 1 holds execute (`ready_to_implement` gated / stop after planner).
- [ ] ROADMAP / SESSION honesty: Multi-experiment remains **Remaining: WorkSpace only** / **not Complete**.
- [ ] STAGE 1 stop after planner — no implementer this run.

## Out of scope

- Implementing / moving / remote-config execute in this STAGE 1 run
- Whole-tree archive of `C:\Project\WorkSpace` under ordinary Continuity
- Continuity A / `docs_only` re-attest as the cycle outcome
- Re-opening Medium / Early / archived Multi-experiment peers as move sources
- Re-litigating taxonomy / must-preserve (unless user changes Q2)
- Dirty-tree auto-commit / forced hygiene (unless user changes Q3)
- Jumping Primary next / Special git / marking Multi-experiment Complete
- History rewrite, force-push, origin URL rewrite by default
- Re-proposing Appendix A isolation for the same parents / re-clearing live multi-remote without regression
