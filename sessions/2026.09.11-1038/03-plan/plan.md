# Plan — Cycle 16 Multi-experiment (`WorkSpace` only / continue-strategy)

**Session:** `sessions/2026.09.11-1038/`  
**Phase:** `03-plan/`  
**Inputs:** `01-prompt-betterment/refined-prompt.md`, `01-prompt-betterment/notes.md`, `02-research/research-brief.md` (+ findings)

---

## Goal restatement

Continue Cycle 16 under Continuity **Q1=A**: keep Multi-experiment locked on **`WorkSpace` only**, and update **org-repo honesty docs** from the Cycle 16 re-probe — attest that live hazards match Cycle 15 (**no material hazard delta**: nested **5**, live hermes **sole `origin`**, whole-tree clearance **NO**), fix the stale `catalogue/inventory.md` “live multi-remote uncleared” line, and optionally refresh opaque `.env*` live count / size-band notes. Zero corpus path moves; zero remote-config; never invent whole-tree archive; never mark the row Complete.

---

## Mutation class

**`docs_only`**

| Rule | This cycle |
| --- | --- |
| Corpus path moves / renames / deletes | **Zero** (INDEX path `C:\Project\WorkSpace` = **verify-only / never re-move**) |
| Remote-config (`git remote remove` / `set-url` / etc.) | **Zero** |
| Org-repo scaffolding / docs / INDEX / inventory / strategy / ROADMAP Notes | **Allowed** (writes inside this organisation git root only) |
| Plan-gate pause before implementer | **N/A** — `docs_only` does not require corpus FS plan gate |
| STAGE 1 | If `ready_to_implement: yes` → same-run implement→audit→self-improver |

**Implementer attestation (required in implementation log):** state explicitly that **zero corpus FS mutations** and **zero remote-config** occurred; list only org-repo files touched; optional verify-only probes (`Test-Path` / Read / Glob / Read `.git/config` equivalent) logged with probe method — no Shell-only hard requirement if Shell unavailable.

---

## Research locks to encode (binding)

| Lock | Value |
| --- | --- |
| Continuity | Q1=**A**, Q2=**A**, Q3=**A** |
| `clearance_whole_tree` | **NO** |
| `nested_live_count` | **5** |
| `multi_remote` | **CLEARED** (sole `origin`; no `cada` regression) |
| INDEX WorkSpace path | `C:\Project\WorkSpace` — **verify-only; never re-move** |
| Appendix A parents | Do **not** re-propose (`_backups` / `_quarantine` already under `archive\hygiene\2026.09.11…`) |
| Taxonomy | **proposed-ratified — ready for user sign-off** (≠ final) |
| Must-preserve | **draft — not auto-locked / for user review** |
| TNA / hermes dirty | Disclose only; **NO_AUTO_COMMIT** |
| ROADMAP | Remaining **`WorkSpace` only** / **not Complete** / no Primary next |

---

## Acceptance criteria

- [ ] Mutation class remains **`docs_only`**; implementation log attests **zero** corpus path moves and **zero** remote-config
- [ ] `program/git-strategy-workspace-hazards.md` gains a **Cycle 16 re-probe attestation** stating: nested **5**; live multi-remote still **CLEARED** (no `cada` regression); `clearance_whole_tree` still **NO**; Appendix A parents unchanged / no re-proposal; Continuity Q1=A; **hazard state = no material delta** vs Cycle 15
- [ ] `catalogue/inventory.md` WorkSpace Notes: stale **“live multi-remote uncleared”** flipped to **Cleared (Cycle 15; confirmed Cycle 16)** + still-at-root / row **not** Complete
- [ ] Recommended honesty (do unless blocked): strategy hazard table opaque `.env*` **17 → 7 live** (path presence only; contents unread); optional cheap size-band note (~1086 MB excl `.git` / ~716 MB also excl `node_modules`) — **still XL / clearance NO** (do not flip clearance)
- [ ] `catalogue/INDEX.md` and `program/ROADMAP.md`: optional Cycle 16 footnote only — path/status already accurate; **must** keep Remaining **`WorkSpace` only** / **not Complete** / **no** Primary next / Special git jump
- [ ] No whole-tree archive invented; no Appendix A re-isolation; no Medium / archived peer reopen as move sources
- [ ] Taxonomy / must-preserve labels **not** upgraded to final / auto-locked (Q2=A)
- [ ] TNA / hermes dirty **not** auto-committed (Q3=A); disclose in log if re-probed
- [ ] Session implementation log written under `sessions/2026.09.11-1038/04-implementation/` (or skill-equivalent path)

**Optional / not hard AC:** historical inventory size `2308.43 MB` refresh; deep archive-hygiene remote inventory.

---

## What the user is approving (plan gate)

**N/A** — this cycle is **`docs_only`**. Continuity Q1=A already locked continue-strategy docs; there is **no** corpus FS / remote-config plan gate. Orchestrator may proceed straight to implementer when `ready_to_implement: yes`.

---

## Ordered steps

### Step 1 — Bootstrap implementation log + verify-only preflight

| | |
| --- | --- |
| **Files/paths** | `sessions/2026.09.11-1038/04-implementation/` (create log); verify-only: `C:\Project\WorkSpace`; strategy artifact |
| **Action** | Open implementation log. Confirm Continuity locks (Q1=A docs_only). Optional cheap verify: `Test-Path`/`Read` WorkSpace still at INDEX path; do **not** move. Log probe method. Disclose hermes dirty only if re-checked — **NO_AUTO_COMMIT**. |
| **Verification** | Log exists; mutation class stated `docs_only`; zero-move intent recorded before edits |

### Step 2 — Required: Cycle 16 attestation on hazard strategy

| | |
| --- | --- |
| **Files/paths** | `program/git-strategy-workspace-hazards.md` |
| **Action** | Append a **Cycle 16** section (do not rewrite Cycle 10–15 history from scratch). Encode research locks: nested **5**; multi_remote **CLEARED**; clearance_whole_tree **NO**; Appendix A verify-only / no re-proposal; Continuity Q1=A; cite session `sessions/2026.09.11-1038/`; state **no material hazard delta** vs Cycle 15. Point “next FAW lock” still WorkSpace-only / not Complete. |
| **Verification** | Section present; clearance still **NO**; no Complete / whole-tree archive language |

### Step 3 — Required: inventory multi-remote honesty fix

| | |
| --- | --- |
| **Files/paths** | `catalogue/inventory.md` (WorkSpace row Notes ~line with “live multi-remote uncleared”) |
| **Action** | Replace stale uncleared wording with **Cleared (Cycle 15; confirmed Cycle 16)**; keep still-at-root / nested **5** / Appendix A / whole tree **not** archived / Multi-experiment **not** Complete. |
| **Verification** | Grep shows no WorkSpace “live multi-remote uncleared”; Cleared + Cycle 16 confirmation present |

### Step 4 — Recommended: opaque `.env*` + optional size-band honesty

| | |
| --- | --- |
| **Files/paths** | `program/git-strategy-workspace-hazards.md` (hazard table `.env*` **17** → **7 live**); optionally `catalogue/inventory.md` note |
| **Action** | Update live opaque `.env*` count to **7** (presence only — never read contents). Optionally note cheap Cycle 16 size band (~1086 MB excl `.git`; ~716 MB also excl `node_modules`) while stressing **still XL / clearance NO**. Do not treat size shrink as whole-tree clearance. |
| **Verification** | Strategy table reflects **7 live** (or explicit “7 live post–Appendix A; historical 17 retired”); clearance remains **NO** |

### Step 5 — Optional: INDEX / ROADMAP Cycle 16 footnote only

| | |
| --- | --- |
| **Files/paths** | `catalogue/INDEX.md` (WorkSpace row / Primary-next note); `program/ROADMAP.md` (Multi-experiment Notes / next-lock hint) |
| **Action** | If touching: add brief Cycle 16 docs_only re-probe footnote (session `2026.09.11-1038`; no material hazard delta; inventory honesty). **Forbidden:** mark Multi-experiment **Complete**; jump Primary next / Special git; reopen Medium / archived peers; invent whole-tree archive destination. |
| **Verification** | Remaining still **`WorkSpace` only**; status **in progress / not Complete**; INDEX path still `C:\Project\WorkSpace` |

### Step 6 — Close implementation log + zero-mutation attestation

| | |
| --- | --- |
| **Files/paths** | Implementation log under `sessions/2026.09.11-1038/04-implementation/` |
| **Action** | List every org-repo file edited; attest zero corpus moves / zero remote-config; record any verify-only probe methods; note taxonomy/must-preserve untouched (still proposed-ratified / draft); note TNA dirty not committed. |
| **Verification** | Attestation present; AC checklist mappable for auditor |

---

## Explicit non-goals

- Whole-tree move / archive of `C:\Project\WorkSpace`
- Any `fs_mutation` or Continuity B scoped isolation this cycle
- Re-proposing Appendix A for `_backups` / `_quarantine`
- Remote-config / `git remote remove` / `set-url` / force-push / history rewrite
- Moving live nested primaries **#1–#5**
- Marking Multi-experiment **Complete** or jumping Primary next / Special git / High / Obsidian / ProjetOrif
- Reopening Medium / Early or archived `PWAExemple` / `GitTest` / `WorkStationPWA` as sources
- Taxonomy final ratification or must-preserve auto-lock
- TNA / hermes dirty auto-commit or forced hygiene
- Org-repo publish / pull (push dual preflight **N/A**)

---

## Rollback / risk notes

| Risk | Mitigation / rollback |
| --- | --- |
| Accidental Complete / Primary next wording | Fail-closed AC; revert ROADMAP/INDEX edits if status flips |
| Inventing whole-tree archive “because multi-remote cleared” | Continuity A + clearance **NO** in Steps 2–5; auditor checks |
| Re-running Appendix A | Parents absent live — verify-only only; no move Steps |
| Leaving inventory “uncleared” while claiming no delta | Step 3 is **required** material catalogue fix |
| Reading `.env*` contents | Path presence only; never open/quote/log secrets |
| Docs-only “fatigue” false Complete | Attestation + named inventory delta = required honesty work, not theater |

**Rollback:** revert org-repo markdown edits only (git checkout / restore those files). No corpus FS rollback needed if Steps followed (zero corpus mutations).

---

## Blocking questions

**none** — Continuity Q1/Q2/Q3 locked; research recommends Option A with clear required deltas; no multi-remote regression; no auth/sync AC this cycle.

---

## Ready to implement

**`ready_to_implement: yes`**

Orchestrator: same-run implement→audit→self-improver (STAGE 1 auto-continue for `docs_only` + ready yes). No mandatory plan-gate pause.
