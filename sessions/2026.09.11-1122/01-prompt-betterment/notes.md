# Prompt betterment notes — Cycle 17

**Session:** `sessions/2026.09.11-1122/01-prompt-betterment/`  
**Status:** Continuity **locked** — `ready_for_researcher: yes` · `stage_1_hold: yes` · same-run implement **FORBIDDEN**  
**Consent UX:** Continuity Qs were explained with plain-language + pros/cons before reply; no jargon-explanation debt.

---

## Continuity (locked — do not re-ask)

| Lock | Value | Source |
| --- | --- | --- |
| Cycle id | Cycle 17 — FAW git-manager end-to-end closing pass (+ leftover Cycle 16 finish-sync) | orchestrator |
| ROADMAP row | Multi-experiment **remaining `WorkSpace` only** — in progress / **not** Complete | orchestrator + ROADMAP |
| Never | Jump Primary next / Special git; reopen Medium / archived peers; invent whole-tree archive; force-move WorkSpace; mark Multi-experiment Complete | orchestrator |
| Strategy artifact | `program/git-strategy-workspace-hazards.md` EXISTS — honor Next FAW lock hint (residual WorkSpace only) | orchestrator |
| Prior session | `sessions/2026.09.11-1038/` complete (audit pass; Continuity Q1=A docs_only) | orchestrator |
| STAGE | **STAGE 1 ONLY** (phases 1–3) → **Hermes plan gate**; do **not** same-run implement even if `docs_only` + ready; STAGE 2 = resume **same folder** after gate yes (+ amendments) | user-named |
| Plan gate content | Plan must include **“What the user is approving”** intent-preview for Hermes | user-named |
| **Q1** | **A** — optional mid-cycle git-manager **+ mandatory final closing pass** after self-improver (`06-audit/**`, `07-self-improvement/**`, `.cursor/**` cycle edits, `SESSION.md` close; same allowlist + GfW/GCM) | user reply |
| **Q2** | **A** — Cycle 16 leftover finish-sync = **first STAGE 2 git pass** | user reply |
| **Q3a** | **A** — carry taxonomy/must-preserve **waive re-litigation** | user reply (confirm) |
| **Q3b** | **A** — carry TNA dirty **defer / NO_AUTO_COMMIT** | user reply (confirm) |
| Mutation class | **`docs_only`** (FAW definition + allowlisted org-repo git finish-sync; zero corpus path moves / zero WorkSpace remote-config) | Continuity + user goal |
| Taxonomy language | proposed-ratified — ready for user sign-off; must-preserve = draft — not auto-locked | program law |
| Primary work | Org-repo **FAW workflow amendment** + Cycle 16 allowlisted finish-sync — **not** WorkSpace corpus move | user goal |

**Not re-asked:** same-run implement (forbidden); WorkSpace A/B/C path pack; Medium/Multi subset packs.

---

## Short Continuity pack (asked; now closed)

Ask summary (durable):

1. Q1 — Mid+final (A) vs final-only (B) vs mid-mandatory+final (C)? → **locked A**
2. Q2 — Include Cycle 16 leftover finish-sync in STAGE 2? → **locked A**
3. Q3 — Carry taxonomy waive + TNA defer? → **locked Q3a=A / Q3b=A**

Full option/tradeoff tables remain above in prior turn artifacts / chat; not re-opened.

---

## Answers

**Raw user Continuity reply:** `A / A / confirm - approve all three.` (plus explicit Q1=A / Q2=A / Q3 carry detail in orchestrator resume)

| ID | Locked | Source | Rationale |
| --- | --- | --- | --- |
| Q1 | **A** — optional mid-cycle git-manager + **mandatory final closing pass** after self-improver (commits/pushes `06-audit/**`, `07-self-improvement/**`, `.cursor/**` cycle edits, `SESSION.md` close; same allowlist + GfW/GCM) | `user reply` | Raw: `A / A / confirm - approve all three.` + explicit Q1=A |
| Q2 | **A** — include Cycle 16 leftover finish-sync as **first STAGE 2 git pass** | `user reply` | Raw letter A + “approve all three”; resume restates Q2=A |
| Q3a | **A** — carry taxonomy/must-preserve waive re-litigation | `user reply` | `confirm` + Q3a=A |
| Q3b | **A** — carry TNA dirty defer (NO_AUTO_COMMIT non-allowlist) | `user reply` | `confirm` + Q3b=A |

**Open asks:** none.

**Bare Continuity approval ≠ Hermes plan-gate execute.** Proceed research → plan → Hermes only.

---

## Assumptions

- Org-repo git root only for stage/commit/push; never sibling trees under multi-root workspace.
- Allowlist unchanged unless plan explicitly extends: `sessions/**`, FAW skill/agents/rules, `AGENTS.md`, `sessions/_templates/**`.
- Windows: prefer Git for Windows + GCM when PATH git is MSYS without helper.
- `stage_1_hold: yes` regardless of `docs_only`; same-run implement **FORBIDDEN**.
- Hermes plan gate ≠ Continuity Choose.

---

## Open risks

- Mid-cycle + final close can leave tiny post-push session notes dirty (known FAW tradeoff) — final pass should minimize orphans; auditor: expected finalize dirt = Low if policy says so.
- STAGE 2 must sequence Q2 finish-sync **before** (or as first step of) FAW amendment so Cycle 16 orphans are not left behind while fixing the workflow.
- Bootstrap stubs in later phase folders (if any) are not Continuity answers.

---

## Self-improvement backlog

- None (questions included explanations + pros/cons before lock).

---

## Key alignment changes (raw → refined)

- Raw “next reorganisation cycle” → primary = **FAW git-manager end-to-end closing pass** (+ Cycle 16 leftover finish-sync), not WorkSpace move.
- Phase-5-only commit → **Q1=A**: mid optional + **mandatory final close** after self-improver.
- **Q2=A**: STAGE 2 starts with Cycle 16 leftover git finish-sync.
- **Q3a/Q3b=A**: taxonomy waive + TNA defer carried.
- STAGE 1 + Hermes → hard hold; no same-run implement.
- ROADMAP WorkSpace-only residual → zero corpus moves; never Complete.
