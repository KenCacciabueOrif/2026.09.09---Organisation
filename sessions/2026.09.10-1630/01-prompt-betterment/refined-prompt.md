# Refined prompt — Cycle 13 Multi-experiment (`WorkSpace` only / continue strategy)

## Goal

Run one FAW program cycle for the orchestrator-locked ROADMAP row **Multi-experiment remaining: `WorkSpace` only**, under **post–hazard-strategy Continuity**: **continue strategy** from the durable artifact (not Cycle 9 pure research+defer; not Appendix A execute).

1. **Anchor on** `program/git-strategy-workspace-hazards.md` (Cycle 10+). Re-probe live `C:\Project\WorkSpace` against that classification; refine strategy docs and INDEX/ROADMAP honesty as evidence warrants. **Classified ≠ cleared** for whole-tree move.
2. **Mutation class this cycle:** **`docs_only`**. Zero corpus path moves/renames/deletes. Do **not** execute Appendix A `_backups`/`_quarantine` isolation (that requires explicit Continuity **B** in a later cycle **plus** a separate plan gate).
3. **Never force-move / whole-tree archive** the XL `WorkSpace` tree under ordinary Multi-experiment Continuity — **OUT OF SCOPE**.
4. **Never** mark Multi-experiment **Complete** or jump **Primary next** / Special git while `WorkSpace` remains.
5. **Never** reopen Medium / Early/simple or re-propose archived peers (`PWAExemple`, `GitTest`, `WorkStationPWA`) as move sources.
6. Continuity / “next cycle” / “continue strategy” / yes→defaults **≠** move approval.
7. **STAGE rule (orchestrator):** Run phases **1–3** (prompt-betterment → research → plan) first. If the plan is **`docs_only`** with **`ready_to_implement: yes`**, continue through implement → audit → self-improver in the **same run**. Otherwise **stop at the plan gate** and wait for user approval. Prompt-betterment does **not** implement.

## Constraints

- **ROADMAP lock:** same Multi-experiment row / `WorkSpace` only — do not re-pick.
- **Continuity carry (Q2=A):**
  - Layout: archive / paused / active per research status
  - Nested `.git`: **atomic** (intact trees; fail-closed unless a gated remediation plan explicitly addresses a named root)
  - Secrets: **opaque `.env`** — path presence only; never read/quote contents
  - Remotes: **SSH-origin path-only** — no `origin` URL rewrite this cycle (document only)
  - Status: 90-day heuristic when classifying (confirm from disk)
  - Windows nested-`.git` lock recovery: only relevant if a **future** approved scoped move runs — prefer single move; on PermissionDenied/split → reunify + `robocopy /E /MOVE`; remove **only empty** leftover `.git` shells; re-attest; log deviation
  - INDEX honesty: document continue-strategy progress; never claim moves or “cleared” hazards without evidence
  - Taxonomy wording: **proposed-ratified — ready for user sign-off** (not final without explicit user approval)
  - Must-preserve wording: **draft — not auto-locked / for user review**
- **Consent:** Continuity / next / continue strategy **≠** move approval. If any future plan is `fs_mutation`, **mandatory separate plan gate** before implementer; include intent-preview. This cycle’s locked path does **not** select Appendix A execute.
- **Mutation class:** **`docs_only`**. Whole XL tree archive under ordinary Multi-experiment Continuity = **OUT OF SCOPE**. Appendix A scoped isolation = **out of this cycle’s Continuity** (available later with Continuity B + plan gate only).
- **Pending gates:** do **not** re-block solely to re-litigate taxonomy final sign-off or must-preserve draft.
- **Org-repo git:** stage/commit only this organisation git root when publishing session docs; no sibling-tree ops.
- Small diffs; no secrets in commits or session logs; no force-push; no history rewrite / `filter-repo`.
- Agent-owned: do not dump user chores to fix remotes/PATH as Continuity requirements.

## Context pointers

- Program: `program/ROADMAP.md`, `program/CHARTER.md`
- **Primary strategy artifact:** `program/git-strategy-workspace-hazards.md`
- Catalogue: `catalogue/INDEX.md`, `catalogue/must-preserve.md` (draft)
- Prior cycle (continue strategy): `sessions/2026.09.10-1247/` (Cycle 12 — docs_only; clearance still NO; Appendix A unused; Next FAW lock hint: continue strategy or Continuity B + plan gate)
- Prior continue-strategy: `sessions/2026.09.10-1047/` (Cycle 11)
- Prior strategy-write cycle: `sessions/2026.09.10-0907/` (Cycle 10)
- Prior defer cycle: `sessions/2026.09.10-0848/` (Cycle 9 — docs_only defer; do not re-default narrative)
- This session: `sessions/2026.09.10-1630/`
- Continuity answers: `sessions/2026.09.10-1630/01-prompt-betterment/notes.md`
- Corpus candidate (only): `C:\Project\WorkSpace`
- Do **not** use as sources: archived Multi-experiment peers; Medium/Early archive or paused paths from prior cycles

## Acceptance criteria

- [ ] Research / plan / implement **anchor on** `program/git-strategy-workspace-hazards.md`; cover **`WorkSpace` only**; archived peers verified not re-proposed as sources.
- [ ] Cycle declared **`docs_only`**; **zero** corpus path moves/renames/deletes (no Appendix A execute).
- [ ] Strategy / INDEX / ROADMAP honesty updated as needed; do **not** claim hazards cleared if only re-documented; do **not** claim moves that did not happen.
- [ ] Multi-experiment row stays **in progress** with **Remaining: `WorkSpace` only** — **never** Complete / Primary next / Special git jump while it remains.
- [ ] **No whole-tree archive / force-move** of `WorkSpace` under ordinary Multi-experiment Continuity.
- [ ] Medium / Early / archived Multi-experiment peers **not** reopened as move sources.
- [ ] Opaque `.env`, atomic nested git, SSH path-only Continuity respected (document-only for remotes this cycle).
- [ ] Taxonomy / must-preserve language stays **proposed-ratified / draft** — not claimed final user ratification; no sole re-block on those gates.
- [ ] No remote URL rewrite / history rewrite / `.env` content reads.
- [ ] **STAGE:** phases 1–3 complete first; if plan is `docs_only` + `ready_to_implement: yes`, implement/audit/self-improve may continue in the same run; if `fs_mutation` or not ready, orchestrator **stops at plan gate**.
- [ ] Auditor can verify AC from session artifacts without chat history.

## Out of scope

- Executing Appendix A scoped `_backups`/`_quarantine` isolation this cycle (needs Continuity B + separate plan gate in a future cycle)
- Whole XL `WorkSpace` tree archive / force-move under ordinary Multi-experiment Continuity
- Moving Special git / ProjetOrif / Obsidian / hygiene mini-batches
- Reopening Medium wrappers or Early/simple batches
- Re-moving or re-proposing `PWAExemple`, `GitTest`, `WorkStationPWA`
- Taxonomy final sign-off or must-preserve final lock (unless user explicitly starts that gate)
- Rewriting remotes, history rewrite, force-push, reading `.env` contents
- Marking Multi-experiment Complete while `WorkSpace` remains
- Treating Continuity / “next” / “continue strategy” as plan-gate move approval
- Re-defaulting to Cycle 9 pure research+defer as the primary path while ignoring the durable strategy artifact
- Dumping user chores to fix remotes/PATH as Continuity requirements
- Auto-continuing past STAGE 1 when the plan is `fs_mutation` or not `ready_to_implement`

## Good vs bad outcomes

| Good | Bad |
| --- | --- |
| Strategy artifact re-probed/refined; INDEX/ROADMAP honest; zero moves; row stays in progress | Appendix A / any corpus move without Continuity B + plan gate |
| Classified ≠ cleared stated clearly | Force-move / whole-tree archive |
| Continue strategy (not Cycle 9 re-defer theater) | Mark Multi-experiment Complete / jump Primary next while WorkSpace remains |
| No peer reopen | Re-propose archived `PWAExemple` etc. as sources |
| Fail-closed on whole-tree; STAGE respected | Silent remote rewrite, `.env` reads, or STAGE skip into FS mutation |

## Locked choices (prompt-betterment)

| Item | Value | Source |
| --- | --- | --- |
| Q1 | **A** — continue strategy from `program/git-strategy-workspace-hazards.md` (`docs_only`; not Appendix A execute; not Cycle 9 pure defer) | `yes→defaults` — raw goal honors Next FAW lock hint (**continue strategy**) |
| Q2 | **A** — carry Continuity | `yes→defaults` |
| Q3 | **A** — do not re-block on taxonomy/must-preserve re-litigation | `yes→defaults` |
| ROADMAP | Multi-experiment **`WorkSpace` only** | orchestrator lock |
| Mutation class | **`docs_only`** | Q1=A |
| Consent | Continuity / continue strategy ≠ plan-gate move approval | Continuity law |
| STAGE | 1–3 first; auto-continue only if `docs_only` + `ready_to_implement: yes` | raw goal |
