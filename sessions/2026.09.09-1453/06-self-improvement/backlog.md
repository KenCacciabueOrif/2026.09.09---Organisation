# Backlog — session `2026.09.09-1453`

Agent-owned only (no recurring “user should check X”).

| ID | Item | Owner | Notes |
| --- | --- | --- | --- |
| D2 | Optional researcher/implementer helper: if `merge-tree` predicts only allowlist paths, auto-annotate Continuity “Q3b=B eligible” in research brief | agent (later FAW) | Policy already in `pull-cycle.md`; tooling nicety only |

## Dropped (would dump work on user)

- Recurring PATH/MSYS checks for the user — agents keep absolute GfW instead.
- “User must resolve allowlist FAW conflicts” as standing next step — replaced by Continuity defaults.
