# Plan — Cycle 19 Continuity X (WorkSpace XL / whole-tree git-strategy)

**Session:** `sessions/2026.09.15-1014/`  
**Phase:** `03-plan/` (STAGE 1 — **STOP for mandatory plan gate**)  
**Locks:** Continuity **X**; Q1=**A**; Q2=**A**; Q3=**A**  
**Prior evidence:** `01-prompt-betterment/refined-prompt.md` · `02-research/research-brief.md` · `program/git-strategy-workspace-hazards.md`

---

## Goal

Deliver a **dedicated XL / whole-tree git-strategy** for Multi-experiment remaining **`WorkSpace` only**: **extend** (do not rewrite) `program/git-strategy-workspace-hazards.md` with Continuity X **keep-vs-split / fate** options and **whole-tree clearance criteria**; attach an **optional held** OS-IA-only `fs_mutation` path map for the plan gate. Continuity **X ≠ execute**. STAGE 1 ends after this plan; **no** corpus path mutation, rename, delete, or remote-config until the user answers the plan gate and STAGE 2 resumes **this same session**. Multi-experiment stays **in progress / Remaining: WorkSpace only** — **never Complete** / Primary next / Special git while `C:\Project\WorkSpace` (or uncleared nested units) remain.

---

## Mutation class

| Field | Value |
| --- | --- |
| Class | **Hybrid (gate-selected):** primary body = **`docs_only`** strategy extension **+** optional held **`fs_mutation`** (`path_batch`, OS-IA-only). Continuity X plan gate chooses **D** / **M** / **D+M** / amend / hold. |
| Mutation kind (if fs_mutation) | `path_batch` — **OS-IA intact relocate only** (not remote-config; not whole-tree; not TNA-internal nest extraction) |
| Continuity gate | Continuity **X** already locked (Cycle 18) — this is the **mandatory plan gate** before any path mutation |
| Corpus FS (STAGE 1) | **Zero** intentional corpus moves/renames/deletes |
| Corpus FS (STAGE 2) | **Only if** gate selects **M** or **D+M** — exact OS-IA map below; else zero |
| User approval before implementer | **Required** — Continuity X + any `fs_mutation`; even **D-only** requires explicit gate yes before STAGE 2 implementer (STAGE 1 hold) |
| First-move gates | Q2=**A** — taxonomy **proposed-ratified — ready for user sign-off** (not final); must-preserve **draft — not auto-locked**; waive re-litigation this cycle. Batch path map still needs this gate. |
| Attest (implementer) | Zero-corpus for **D**; for **M**: destination `Test-Path` + nested `.git` intact + remotes path-only + worktree re-probe on **nested clone path** `…\OS-IA` only; log probe method |

**Honesty:** This is **not** Continuity A theater — material advance = fate matrix + clearance criteria (+ optional concrete held map). This is **not** silent whole-tree archive. This is **not** execute authority.

---

## Context (binding)

| Item | Status |
| --- | --- |
| Nested live roots | **5** atomic units: `OS-IA`, `TestNewWorkspaceAgent` (TNA), `hermes-agent`, `orchestrateur`, `WorkshopOrif` |
| Live multi-remote | **CLEARED** (Cycle 15; confirmed Cycle 16) — do not re-clear without regression |
| Appendix A parents | **Isolated** Cycle 14 under `archive\hygiene\…` — **do not re-propose** |
| Whole-tree clearance | **NO** — classified ≠ cleared |
| Dirty WT (disclose) | TNA **2307**, OS-IA **921**, hermes **56**, orchestrateur **60**, WorkshopOrif **119** — **Q3=A NO_AUTO_COMMIT** |
| Size (cheap, if available) | ~1086 MB excl `.git` — XL; shrink ≠ clearance flip |
| Peers | Medium / Early / archived `PWAExemple` / `GitTest` / `WorkStationPWA` = **verify-only** — never move sources |

### Continuity X fate defaults (to encode in strategy docs under **D**)

| Unit / envelope | Default fate (docs) | Notes |
| --- | --- | --- |
| Non-git wrapper `C:\Project\WorkSpace` | **Keep at root** until clearance criteria met or dedicated gated fate | Never silent whole-tree archive |
| TNA envelope (#2–#5) | **Keep at root** interim | Inner nest extract needs dedicated **parent-surgery** plan (dirty TNA) — **out of this held map** |
| `OS-IA` (#1) | **Keep at root** unless gate selects **M** | Cleanest first-split candidate; optional held map |
| Eventual archive (whole WorkSpace) | **Future gated fate only** after clearance criteria flip **NO → yes** | Forbidden without clearance + explicit gate |

### Proposed whole-tree clearance criteria (docs — flip **NO → yes** only when all true)

Document in hazard artifact (not invent silent clear):

1. Live multi-remote remains **CLEARED** (no `cada` / unexpected multi-remote regression on live nests).
2. Linked worktrees on every in-scope nested clone = **expected only** (re-probe immediately before any whole-tree gate).
3. User has **explicitly** chosen whole-tree or full-split fate at a dedicated plan gate (not Continuity / Choose alone).
4. Parent-surgery policy for dirty TNA is **written and approved** if any in-TNA nest moves are in map.
5. Opaque `.env*` inventory (presence-only) re-attested for units in map.
6. Program honesty: Multi-experiment Complete / Primary next only when **no** WorkSpace remainder at root **and** locked fate for every atomic unit is executed or explicitly parked — **OS-IA move alone never Completes the row**.

---

## What the user is approving

Plain-language intent preview for the orchestrator plan gate. Continuity X / Choose **≠** this gate.

### Options

| Option | What “yes” commits to | Corpus FS | Pros | Cons / tradeoffs |
| --- | --- | --- | --- | --- |
| **D** | STAGE 2 implementer **extends** `program/git-strategy-workspace-hazards.md` with Continuity X section: fate matrix (keep / split / eventual-archive), clearance criteria, OS-IA-first-subset pointer, Cycle 19 attestation; refresh ROADMAP Notes / INDEX / inventory honesty as needed — **zero corpus moves** | None | Material strategy advance; safest; matches Continuity X docs half; same-run docs_only after gate | No disk relocate yet; WorkSpace surface unchanged |
| **M** | STAGE 2 also (or only) executes **OS-IA-only** intact `Move-Item` per exact map below (destination class **archive** default, or amend to **paused**) | OS-IA path only | Shrinks WorkSpace to TNA-only child; does not tear dirty TNA nests; remotes path-only | Bookmarks/shortcuts to old path break; OS-IA dirty **921** moves with tree (**NO_AUTO_COMMIT**); row still **not** Complete; reverse-move needed to undo |
| **D+M** | Do **D** then **M** in one STAGE 2 resume (docs first, then OS-IA move, then catalogue/ROADMAP honesty) | OS-IA after docs | One gate covers full Q1=A hybrid | Larger blast radius; review both slices carefully |
| **Hold / amend** | No STAGE 2 implement yet; revise map/fate text and re-gate | None | Safest if unsure | Delays strategy write and/or first subset |

**Default recommendation at gate (not auto-execute):** **D+M** with OS-IA → **archive** naming below — or **D** alone if user wants fate text before any move.

### Exact held `fs_mutation` map (proposal-only until **M** / **D+M**)

| Field | Value |
| --- | --- |
| Mutation kind | `path_batch` — intact atomic nested-git relocate |
| Source | `C:\Project\WorkSpace\OS-IA` |
| Destination (default **M-a**) | `C:\Project\archive\2026.09.15 - OS-IA` |
| Destination (amend **M-p**) | `C:\Project\paused\2026.09.15 - OS-IA` |
| Unit | Nested `.git` root **OS-IA** only — move entire tree intact |
| Remotes | **Path-only** — no `set-url` / origin rewrite / force-push |
| Worktree probe | Immediately before move: `git worktree list` on **`C:\Project\WorkSpace\OS-IA`** (nested clone path) — fail-closed if unexpected >1 |
| Opaque `.env*` | None listed on OS-IA in Cycle 19 live list — re-attest presence-only; never read contents |
| Dirty WT | Disclose OS-IA porcelain (~921) — **NO_AUTO_COMMIT** (Q3=A) |
| Parents | Create `C:\Project\archive` or `C:\Project\paused` if missing (org convention); do **not** create Appendix A hygiene parents |
| Out of map | Whole `WorkSpace`; TNA; hermes / orchestrateur / WorkshopOrif; Appendix A re-move; hermes remote-config; archive-hygiene `cada`; Medium/Early/PWAExemple peers |
| After success | Update INDEX / inventory / ROADMAP Notes: OS-IA new path; WorkSpace still at root with TNA; Multi-experiment **Remaining: WorkSpace only** — **not** Complete |
| Reverse-move note | Log reverse: destination → `C:\Project\WorkSpace\OS-IA` (manual restore path) |

### What “no / edit” means

No strategy-doc write beyond what’s already on disk for this cycle’s STAGE 2, and **no** OS-IA move, until a revised plan gate. Session stays `in_progress` at plan gate; do not mark Complete.

---

## Exact user-gate question (orchestrator copy-paste)

```
Cycle 19 Continuity X — plan gate (STAGE 1 hold → STAGE 2 only after your answer)

Plain language: We researched WorkSpace again. Hazards are unchanged (5 nested git roots; live multi-remote still cleared; whole-tree archive still NOT cleared). Continuity X asks for a real whole-tree strategy write-up — not another empty honesty re-check — and a hard stop before any files move.

This plan proposes two slices you can approve together or separately:

  D — Docs only: extend program/git-strategy-workspace-hazards.md with keep-vs-split / fate options and clearance criteria (what would have to be true before whole-tree archive is allowed). Zero file moves under C:\Project\WorkSpace.
  M — Move only OS-IA: relocate C:\Project\WorkSpace\OS-IA intact to
        default: C:\Project\archive\2026.09.15 - OS-IA
        (or say “paused” → C:\Project\paused\2026.09.15 - OS-IA)
      Remotes stay as-is (path-only). Dirty files in OS-IA are NOT auto-committed. WorkSpace / Multi-experiment stay unfinished (TNA + nests remain).

Pros / cons:
  D: safest strategy advance; no bookmarks break; no disk relocate yet.
  M: cleans the only easy sibling out of WorkSpace; bookmarks to the old OS-IA path break; does NOT finish Multi-experiment; does NOT authorize whole-tree archive.
  D+M: one resume does both (docs first, then move).
  Hold/amend: change destinations or drop M.

Forbidden unless you explicitly expand the map later: whole WorkSpace archive; moving TNA or nests inside it; redoing Appendix A; changing git remote URLs.

Reply with one of:
  D
  M   (optional: archive | paused — default archive)
  D+M (optional: archive | paused — default archive)
  hold
  amend: <what to change>

“Yes” without D/M is not enough — pick a letter. Continuity X is not execute permission by itself.
```

---

## Acceptance criteria

### STAGE 1 (this plan — orchestrator / planner)

- [x] Plan cites `program/git-strategy-workspace-hazards.md` and research nested **5** / multi-remote **CLEARED** / clearance_whole_tree **NO**.
- [x] Fate / keep-vs-split / clearance criteria drafted for strategy extension (**D**).
- [x] Optional held OS-IA `fs_mutation` map named with exact source/destination (**M**).
- [x] Mutation class honest: `docs_only` ± held `fs_mutation`; Continuity X ≠ execute.
- [x] `ready_to_implement: no` until plan-gate yes.
- [x] Exact user-gate question with plain language + pros/cons.
- [ ] Orchestrator presents gate and records answer in SESSION / notes before STAGE 2.

### STAGE 2 — if gate **D** or **D+M** (`docs_only` slice)

- [ ] Extend `program/git-strategy-workspace-hazards.md` with **Continuity X / Cycle 19** section: fate matrix; clearance criteria; default keep TNA envelope; optional OS-IA-first pointer; Cycle 19 live attestation (nested 5, multi-remote CLEARED, clearance NO, dirty disclose NO_AUTO_COMMIT); prior Cycle 10–16 history retained.
- [ ] ROADMAP Notes / Next FAW hint: still Multi-experiment **Remaining: WorkSpace only** — **not** Complete; **not** Primary next / Special git.
- [ ] Catalogue honesty (INDEX / inventory) updated for strategy Continuity X pointer if needed — **no** false Complete; taxonomy remains **proposed-ratified — ready for user sign-off**; must-preserve **draft — not auto-locked**.
- [ ] Implementer log attests **zero corpus path moves** for the docs slice (probe method logged).
- [ ] Session artifacts under `sessions/2026.09.15-1014/04-implementation/` etc.

### STAGE 2 — if gate **M** or **D+M** (`fs_mutation` slice)

- [ ] Pre-move: `git worktree list` on `C:\Project\WorkSpace\OS-IA` only — fail-closed if unexpected.
- [ ] Intact move source → approved destination (**M-a** or **M-p**); nested `.git` remains; remotes path-only (no URL rewrite).
- [ ] Post-move: source absent; destination present (`Test-Path` or Read/Glob equivalent — log method); reverse-move note in implementation log.
- [ ] **NO_AUTO_COMMIT** of OS-IA / parent dirty WT (Q3=A).
- [ ] INDEX / inventory / ROADMAP: OS-IA new path; WorkSpace remains; Multi-experiment **not** Complete.
- [ ] Zero whole-tree archive; zero TNA / nest moves; zero peer reopen; zero Appendix A re-proposal; zero remote-config unless separately gated (not this map).

### Always (any STAGE 2 outcome)

- [ ] Never mark Multi-experiment Complete while WorkSpace remains.
- [ ] Never silent whole-tree archive; never force-move beyond approved map.
- [ ] Dual mid + final git-manager only for **org-repo** allowlisted dirt (strategy/docs/session) — not sibling `C:\Project` trees; publish N/A unless later Continuity adds it (this plan: org-repo commit of allowlisted docs/session only if cycle continues through git phases).

---

## Steps

### STAGE 1 (done when plan + gate presented)

0. **Orchestrator** — Persist this `03-plan/plan.md`; set SESSION plan phase done; **present Exact user-gate question**; **do not** launch implementer until reply ∈ {D, M, D+M} (or clear amend then re-gate).  
   - Verify: SESSION `Pending user gates` = plan gate; `ready_to_implement` remains no.

### STAGE 2A — Docs slice (only if gate **D** or **D+M**)

1. **Extend hazard strategy** — Path: `program/git-strategy-workspace-hazards.md`  
   - Action: Append Continuity X / Cycle 19 section (do not delete prior attestations): fate matrix; clearance criteria; keep-at-root default for TNA envelope; OS-IA optional first-subset; live re-probe summary; forbid whole-tree without clearance; Next FAW hint still WorkSpace only.  
   - Verify: File contains Continuity X fate + clearance; clearance_whole_tree still **NO**; Appendix A / multi-remote history intact.

2. **Program / catalogue honesty** — Paths: `program/ROADMAP.md`, `catalogue/INDEX.md`, `catalogue/inventory.md` (as needed)  
   - Action: Notes — Continuity X strategy extended Cycle 19; Remaining WorkSpace only; not Complete; if **M** not approved, state OS-IA still under WorkSpace.  
   - Verify: No Complete / Primary next / Special git; peers not reopened.

3. **Implementation log (docs)** — Path: `sessions/2026.09.15-1014/04-implementation/`  
   - Action: Log zero corpus FS; probe method; files touched.  
   - Verify: Attestation present before mid git.

### STAGE 2B — OS-IA move slice (only if gate **M** or **D+M**; after 2A if D+M)

4. **Pre-move probes** — Path: `C:\Project\WorkSpace\OS-IA`  
   - Action: `Test-Path` source; `git worktree list` on nested clone path only; remotes inventory (names/schemes only); disclose dirty; confirm destination parent; confirm destination absent.  
   - Verify: Unexpected worktrees → **abort** (fail-closed); log probes.

5. **Intact relocate** — Source → destination per gate (**M-a** default or **M-p**)  
   - Action: Prefer `Move-Item`; on Windows lock → reunify + `robocopy /E /MOVE`; remove only empty leftover `.git` shells; no remote rewrite; no auto-commit.  
   - Verify: Source gone; destination exists with `.git`; remotes unchanged (path-only).

6. **Post-move honesty** — Paths: INDEX, inventory, ROADMAP Notes, implementation log  
   - Action: Record new OS-IA path; reverse-move note; WorkSpace still root with TNA; Remaining WorkSpace only; **not** Complete.  
   - Verify: Auditor can grep not-Complete + new path.

### STAGE 2C — Cycle close path (after approved slices)

7. **Mid git-manager** (org-repo allowlist only) — stage/commit/push allowlisted strategy + catalogue + session implementation dirt per FAW publish rules (GfW/GCM).  
   - Verify: No staging of `C:\Project\WorkSpace\**` or extension product paths.

8. **Auditor** → persist `06-audit/report.md` (orchestrator writes returned body).

9. **Self-improver** (mandatory) → `07-self-improvement/`.

10. **Final closing-pass git-manager** — allowlisted late dirt (`06-audit/**`, `07-self-improvement/**`, SESSION close, cycle `.cursor/**`).  
    - Verify: Do not mark session `complete` if final pass skipped while late allowlisted dirt remains; honest `blocked` if push/sync fails.

**Auth/preflight:** N/A for corpus move; for org-repo publish in steps 7/10 use dual preflight (GfW + GCM) per FAW — fail-closed `agent_environment` vs `user_credentials`; missing `gh` alone ≠ credentials missing.

---

## Non-goals

- Executing any corpus / remote-config mutation in STAGE 1
- Silent or ordinary whole-tree `WorkSpace` → archive
- Moving TNA or extracting hermes / orchestrateur / WorkshopOrif without a dedicated parent-surgery plan
- Re-proposing Appendix A `_backups` / `_quarantine` isolation
- Re-clearing live hermes multi-remote without regression evidence
- Marking Multi-experiment Complete or jumping Primary next / Special git
- Reopening Medium / Early / archived Multi-experiment peers as move sources
- Continuity A docs_only re-attest theater (empty re-probe as cycle outcome)
- History rewrite / filter-repo / force-push / origin `set-url`
- Dirty-tree hygiene commits (Q3=A)
- Taxonomy final ratification or must-preserve auto-lock (Q2=A)
- Sibling-tree staging outside this organisation git root

---

## Risks / rollback

| Risk | Mitigation |
| --- | --- |
| Treating Continuity X as execute | STAGE 1 stop; `ready_to_implement: no` until gate letter |
| Continuity A theater | Fate + clearance (+ optional map) = material advance |
| Silent whole-tree archive | clearance **NO**; map forbids it |
| OS-IA move → false Complete | AC + ROADMAP: Remaining WorkSpace / TNA remain |
| TNA parent surgery creep | Held map = OS-IA only; #3–#5 OOS |
| Bookmarks break after M | Disclose in gate pros/cons; reverse-move note |
| Dirty WT auto-commit temptation | Q3=A NO_AUTO_COMMIT; disclose counts |
| Windows file lock mid-move | Reunify + robocopy /E /MOVE; empty `.git` shells only |
| Rollback OS-IA | Reverse-move destination → `C:\Project\WorkSpace\OS-IA`; restore catalogue paths |
| Rollback docs | Revert org-repo commit / restore prior hazard artifact section |
| Overclaim taxonomy / must-preserve | Wording: proposed-ratified / draft only |

---

## Ready to implement

**no** — until plan-gate reply selects **D**, **M**, or **D+M** (with destination class if M). Then orchestrator resumes **same session** STAGE 2 for the approved slice(s) only.

| Gate reply | STAGE 2 mutation class | Implementer scope |
| --- | --- | --- |
| **D** | `docs_only` | Steps 1–3, then 7–10 |
| **M** | `fs_mutation` | Steps 4–6 (skip 1–3 unless user also wants minimal ROADMAP honesty — prefer require **D+M** if docs missing Continuity X section), then 7–10; if hazard artifact still lacks Continuity X section, orchestrator should treat **M-only** as amend→prefer **D+M** or run minimal honesty pointers only |
| **D+M** | `docs_only` then `fs_mutation` | Steps 1–6 then 7–10 |
| **hold** / unclear | none | Stay STAGE 1; do not implement |

**Planner note on M-only:** Prefer user pick **D+M** so fate/clearance land in the durable artifact in the same cycle. If user insists **M** alone, implementer may move OS-IA **and** must still update ROADMAP/INDEX honesty, but must **not** skip documenting that Continuity X strategy text remains incomplete — fail-closed suggest follow-up docs Continuity rather than invent full rewrite mid-move.

---

## Blocking questions

**Plan gate (blocking implementer):** user must answer **D** / **M** / **D+M** / **hold** / **amend** using the Exact user-gate question above.

No other open product decisions for STAGE 1. Destination class defaults to **archive** unless user says **paused**.
