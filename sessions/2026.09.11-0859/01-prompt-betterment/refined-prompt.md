# Refined prompt — Cycle 15

## Goal

Advance Multi-experiment **`WorkSpace` only** past the last remaining **live hermes multi-remote** FS hazard: `origin` + `cada` HTTPS on the **live hermes clone** under `C:\Project\WorkSpace`.

Research must inventory remotes and evidence-pick **one** safe remediation (examples: remove/redirect a dead or duplicate remote; isolate the live clone via linked worktree or relocation). Planner must produce an **`fs_mutation`** (or fail-closed) plan for that single narrow scope. **STAGE 1 stops at the Hermes plan gate** — Continuity ≠ execute; no implement until human yes.

**Rationale locked:** Project must **advance** — no further `docs_only` re-attest theater (Cycles 10–13 already did that; Cycle 14 executed Appendix A FS isolation). If remediation is unsafe, the plan must state the **concrete blocker** and the **minimal gated unblock step** — not re-attest.

## Constraints

### ROADMAP / program (hard)

- Lock Multi-experiment **remaining `WorkSpace` only** — row stays **in progress / not Complete**.
- **Never** mark Multi-experiment Complete; **never** jump Primary next / Special git while WorkSpace remains.
- **Never** reopen archived Multi-experiment peers (`PWAExemple`, `GitTest`, `WorkStationPWA`, …) or Medium/Early as move sources.
- **Never** invent whole-tree WorkSpace archive/move under ordinary Continuity.
- Point all phases at durable artifact: `program/git-strategy-workspace-hazards.md`.
- Prior session Continuity: `sessions/2026.09.11/` (Cycle 14 Appendix A executed — nested 7→5; `_backups`/`_quarantine` at `C:\Project\archive\hygiene\2026.09.11`).

### Narrow binding scope (Q1=B) — IN

- Target: **live hermes multi-remote hazard only** (origin+cada on the live hermes clone under WorkSpace).
- Research: remote inventory + safe options with evidence; pick **one** remediation.
- Plan class: **`fs_mutation`** for the chosen remediation **or** fail-closed plan with concrete blocker + minimal gated unblock — **never** docs_only re-attest theater.
- STAGE 1: phases 1–3 only this run → **Hermes plan gate stop** (no same-run implement/audit/self-improve).

### Explicitly FORBIDDEN this cycle

- Whole-tree WorkSpace archive or move.
- Moving live primaries **#1–#5**.
- Re-proposing `_backups` / `_quarantine` (they live under `archive/hygiene/2026.09.11` — they did **not** reappear under live WorkSpace).
- Any history rewrite or force-push.
- Any change that breaks nested `.git` integrity (roots stay atomic; no rewrite of remote URLs unless the gated map explicitly and narrowly requires a documented remote-config edit that is not force-push/history rewrite).
- Force-move XL WorkSpace; soft-deferred finalize-by-default; Cycle 9 pure defer as the cycle outcome.

### Continuity locks

| Q | Lock | Source |
| --- | --- | --- |
| Q1 | **B** — narrow `fs_mutation` for live hermes multi-remote remediation (binding scope above) | Hermes / user |
| Q2 | **A** — waive taxonomy/must-preserve re-litigation (carry **proposed-ratified** / **draft**) | Hermes / user |
| Q3 | **A** — defer TNA dirty-tree hygiene to a later Continuity | Hermes / user |

- Taxonomy remains **proposed-ratified — ready for user sign-off** (≠ final).
- Must-preserve remains **draft — not auto-locked / for user review**.
- Bare Continuity / Choose ≠ plan-gate approval.

## Context pointers

- `program/ROADMAP.md` — Multi-experiment remaining WorkSpace only
- `program/git-strategy-workspace-hazards.md` — authoritative hazard strategy + Appendix A done note
- `sessions/2026.09.11/` — Cycle 14 Continuity B Appendix A executed
- `sessions/2026.09.11-0859/` — this Cycle 15 session (STAGE 1)
- Candidate root: `C:\Project\WorkSpace` (live hermes clone path to be confirmed by research)
- Archived hygiene: `C:\Project\archive\hygiene\2026.09.11` (Appendix A parents — verify-only)

## Acceptance criteria / verification

1. Research documents live hermes remotes (`origin`, `cada`, URLs, fetch status / dead-vs-live evidence) without reading secret file contents; opaque `.env*` path-presence only if encountered.
2. Research lists remediation options with pros/risks and **selects one** with evidence — or documents why all options fail-closed.
3. Plan declares mutation class **`fs_mutation`** with a narrow map **or** fail-closed with **concrete blocker** + **minimal gated unblock step** — **not** a docs_only re-attest.
4. Plan / research never propose: whole-tree archive; moves of primaries #1–#5; re-move of `_backups`/`_quarantine`; history rewrite; force-push; nested `.git` integrity breakage.
5. Plan includes intent-preview (“What the user is approving”) suitable for Hermes relay + informed-consent pros/cons.
6. `ready_to_implement` may be `yes` for a gated map, but orchestrator **must stop** at Hermes plan gate (STAGE 1) — no implement this run.
7. ROADMAP / INDEX honesty updates may be listed as **post-gate** implementer steps only; do not claim Multi-experiment Complete or Primary next.
8. Session artifacts under `sessions/2026.09.11-0859/02-research/` and `03-plan/` satisfy the above; no corpus FS mutation executed in STAGE 1.

## Out of scope

- Same-run implement / audit / self-improve (STAGE 2 only after Hermes yes + amendments).
- TNA dirty-tree hygiene (Q3=A — later Continuity).
- Taxonomy / must-preserve sign-off litigation (Q2=A — waived this cycle).
- Whole-tree clearance / Multi-experiment Complete / Special git.
- Remote URL rewrite as a general policy (path-only Continuity remains unless the narrow gated map explicitly documents a non-destructive remote-config change).
- User chores to “fix remotes/PATH” as Continuity substitutes — agent-owned research + gated plan only.
