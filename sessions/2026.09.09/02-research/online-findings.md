# Online findings

- Custom subagents: `.cursor/agents/*.md` with YAML `name`, `description`, optional `model`, `readonly`, `is_background` — https://cursor.com/docs/subagents
- Skills: project `.cursor/skills/<name>/SKILL.md` with WHAT+WHEN description
- Rules: `.cursor/rules/*.mdc` with `alwaysApply` / globs
