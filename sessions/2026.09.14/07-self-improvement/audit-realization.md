# Realization & process audit

## Good points

- **Root-cause → plan → deliver** stayed coherent: research pinned kube AUTHOR width 39 truncate; plan named primary fork + allowed fallback; implementer used planned ensui path when secondfry CLI failed (`02-research/`, `03-plan/plan.md`, `04-implementation/log.md`).
- **Mutation class honesty:** Cycle correctly used `product_settings` (not corpus `fs_mutation`); plan-gate marked n/a; zero ROADMAP/WorkSpace churn (`SESSION.md`, `03-plan/plan.md`, audit AC).
- **Fallback discipline:** Primary Marketplace-only ID unavailable via Cursor CLI/Open VSX → documented fallback install without inventing untrusted VSIX (`04-implementation/log.md`).
- **Scope control:** Session docs mid-pushed only (Q8=A); fixture header-only; settings email not shortened; no kube source patch as “fix” (`05-git/log.md`, audit).
- **Resume after interrupt worked in spirit:** User “retry” after implementer/orchestrator stop; 04 artifacts already complete → cycle continued at mid git rather than redoing product work (`SESSION.md` Resume + Implementer outcome).
- **Audit quality:** `pass_with_issues`, 0 Critical; soft Reload / leftover folder / template-math regenerate disclosed as Low; auditor readonly + orchestrator-persisted report (`06-audit/report.md` Meta).
- **Dual git intent:** Mid pass complete with note that final closing pass still required for 06/07/SESSION (`05-git/log.md`).

## Bad points

- **`product_settings` under-specified in FAW law:** Agents/skill/templates mostly binary `docs_only` | `fs_mutation`. Same-run continue + plan-gate n/a for editor/extension cycles relied on planner improvisation — risk of false `fs_mutation` gate or mistaken pause (`03-plan/plan.md` vs `.cursor/agents/planner.md`).
- **Open VSX vs Marketplace not encoded:** Recurring Cursor CLI failure mode (Marketplace-only ID → “not found” / Open VSX 404) lived only in this session’s research/impl log — next cycle can re-discover the hard way.
- **Interrupt / “retry” resume rule incomplete:** General resume-same-session exists, but not explicit “if `04-implementation/` already complete → do **not** relaunch implementer; pick up at next incomplete phase (usually mid git).” User had to say retry after stop (`SESSION.md`).
- **Mid-cycle SESSION checklist lag:** Auditor Low — Workflow progress still showed mid-git incomplete while `05-git/log.md` mid was done (bookkeeping debt before auditor launch).
- **Soft Reload framed as leftover user chore:** Necessary tip, but without workflow language it can look like Critical / `rework_owner: user` — should stay soft optional, agent-owned verify where possible (`06-audit/report.md` Recommended next actions).
- **Agent cannot live Command Palette insert:** Fixture regenerated from template math — honest Low, but product_settings cycles should prefer that as allowed verify method when CLI cannot invoke commands.

## Evidence

| Claim | Pointer |
| --- | --- |
| product_settings + plan-gate n/a | `SESSION.md` Program framing; `03-plan/plan.md` Mutation class |
| secondfry fail → ensui fallback | `04-implementation/log.md` Steps / Extension IDs |
| Resume after interrupt | `SESSION.md` Resume; Workflow progress mid→audit |
| Mid checklist lag | `06-audit/report.md` Gaps + Severity Low |
| Auditor persist | `06-audit/report.md` Meta `write_status: blocked_returned_inline` |
| Mid git not final | `05-git/log.md` Mid attestations |
| Soft Reload remaining | `04-implementation/log.md` User-mediated; audit Recommended |
