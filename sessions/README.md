# Sessions

Each agent cycle writes a folder named `yyyy.mm.dd` (or `yyyy.mm.dd-HHMM` if that date already exists).

Seed new sessions from `_templates/`. See `.cursor/skills/full-agent-workflow/references/session-structure.md`.
