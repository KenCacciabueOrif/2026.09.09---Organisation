# Git-strategy — WorkSpace hazards & remediation

**Program:** organise `C:\Project` · Multi-experiment remaining **`WorkSpace` only**  
**Cycle 10 session:** [`../sessions/2026.09.10-0907/`](../sessions/2026.09.10-0907/)  
**Cycle 11 session:** [`../sessions/2026.09.10-1047/`](../sessions/2026.09.10-1047/)  
**Cycle 12 session:** [`../sessions/2026.09.10-1247/`](../sessions/2026.09.10-1247/)  
**Cycle 13 session:** [`../sessions/2026.09.10-1630/`](../sessions/2026.09.10-1630/)  
**Research:** [`../sessions/2026.09.10-0907/02-research/`](../sessions/2026.09.10-0907/02-research/) · Cycle 11 re-probe [`../sessions/2026.09.10-1047/02-research/`](../sessions/2026.09.10-1047/02-research/) · Cycle 12 re-probe [`../sessions/2026.09.10-1247/02-research/`](../sessions/2026.09.10-1247/02-research/) · Cycle 13 re-probe [`../sessions/2026.09.10-1630/02-research/`](../sessions/2026.09.10-1630/02-research/)  
**Mutation class (Cycle 10–13):** **`docs_only`** — zero corpus moves; this file is strategy + honesty, not an execute map.

**Taxonomy:** **proposed-ratified — ready for user sign-off** (not final).  
**Must-preserve:** **draft — not auto-locked / for user review** (Medium caution for WorkSpace; not sole gate).

---

## Cycle 13 re-probe attestation (2026-09-10 / `sessions/2026.09.10-1630/`)

| Field | Value |
| --- | --- |
| Continuity | **Q1=A** — continue strategy from this artifact (`docs_only`); **not** Appendix A execute; **not** Cycle 9 pure research+defer theater |
| Live result | **No material delta** vs Cycle 12 attestation / Cycles 10–11 classification below |
| Nested roots | Still **7** (5 primary / 2 backup+quarantine hermes clones) |
| Multi-remote | Still `origin`+`cada` HTTPS on live + `_backups` + `_quarantine` hermes trees |
| HEAD divergence | Live hermes `4fbff573…` ≠ clone `8d60d929…` (unchanged class) |
| Linked worktrees | Still **none** on live hermes |
| Opaque `.env*` | Still present — **17** paths (path presence only; contents unread) |
| Size | XL **~5786 MB** re-measured (structure unchanged) |
| **Clearance for whole-tree archive** | Still **NO** — **classified ≠ cleared** |
| Appendix A | Still **unused** / not Continuity-selected this cycle |
| Corpus FS | **Zero** moves/renames/deletes; `_backups` / `_quarantine` remain under WorkSpace |

Hazard tables, recommended fate, and safe rules below remain the authoritative baseline (honesty refine only). Prior Cycle 11–12 attestations retained.

---

## Cycle 12 re-probe attestation (2026-09-10 / `sessions/2026.09.10-1247/`)

| Field | Value |
| --- | --- |
| Continuity | **Q1=A** — continue strategy from this artifact (`docs_only`); **not** Appendix A execute; **not** Cycle 9 pure research+defer theater |
| Live result | **No material delta** vs Cycle 11 attestation / Cycle 10 classification below |
| Nested roots | Still **7** (5 primary / 2 backup+quarantine hermes clones) |
| Multi-remote | Still `origin`+`cada` HTTPS on live + `_backups` + `_quarantine` hermes trees |
| HEAD divergence | Live hermes `4fbff573…` ≠ clone `8d60d929…` (unchanged class) |
| Linked worktrees | Still **none** on live hermes |
| Opaque `.env*` | Still present (path presence only; contents unread) |
| Size | XL band **carried** (~5786 MB Cycle 10; structure unchanged) |
| **Clearance for whole-tree archive** | Still **NO** — **classified ≠ cleared** |
| Appendix A | Still **unused** / not Continuity-selected this cycle |
| Corpus FS | **Zero** moves/renames/deletes; `_backups` / `_quarantine` remain under WorkSpace |

Hazard tables, recommended fate, and safe rules below remain the authoritative baseline (honesty refine only). Prior Cycle 11 attestation retained.

---

## Cycle 11 re-probe attestation (2026-09-10 / `sessions/2026.09.10-1047/`)

| Field | Value |
| --- | --- |
| Continuity | **Q1=A** — continue strategy from this artifact (`docs_only`); **not** Appendix A execute; **not** Cycle 9 pure research+defer theater |
| Live result | **No material delta** vs Cycle 10 classification below |
| Nested roots | Still **7** (5 primary / 2 backup+quarantine hermes clones) |
| Multi-remote | Still `origin`+`cada` HTTPS on live + `_backups` + `_quarantine` hermes trees |
| HEAD divergence | Live hermes `4fbff573…` ≠ clone `8d60d929…` (unchanged class) |
| Linked worktrees | Still **none** on live hermes |
| Opaque `.env*` | Still present (path presence only; contents unread) |
| Size | XL band **carried** (~5786 MB Cycle 10; structure unchanged) |
| **Clearance for whole-tree archive** | Still **NO** — **classified ≠ cleared** |
| Appendix A | Still **unused** / not Continuity-selected this cycle |
| Corpus FS | **Zero** moves/renames/deletes; `_backups` / `_quarantine` remain under WorkSpace |

Hazard tables, recommended fate, and safe rules below remain the authoritative baseline (honesty refine only).

---

## Scope

- Candidate: `C:\Project\WorkSpace` only (still at INDEX root).
- Archived Multi-experiment peers (`PWAExemple`, `GitTest`, `WorkStationPWA`) and Medium / Early paths are **verify-only** — never re-propose as move sources.
- Whole-tree Multi-experiment archive of WorkSpace is **OUT OF SCOPE** until hazards are cleared under a **future gated** strategy.
- Continuity / Choose **≠** plan-gate move approval.

---

## Hazard classification (live 2026-09-10 / Cycle 10)

| Hazard | Classification | Notes |
| --- | --- | --- |
| Multi-remote | **Live primary + clones** | `hermes-agent` (live, `_backups`, `_quarantine`) each have remotes **`origin` + `cada`** (HTTPS). Other five roots: single `origin` HTTPS. |
| Unexpected nested roots | **Backup clone + quarantine clone** | Extra `.git` under `_backups\…\hermes-agent` and `_quarantine\…\hermes-agent`. |
| Primary vs clone | **5 primary / 2 clone** | Live hermes HEAD ≠ backup/quarantine HEAD (clones share a different commit; not a simple ancestor snapshot). |
| Nested-root count | **7** | Unchanged vs Cycle 9; older inventory “3” was docs undercount only. |
| Linked worktrees | **None** | Not a current blocker; re-probe before any future move. |
| Size | **XL ~5786 MB** | `_backups` ~1662 MB; `_quarantine` ~1662 MB (cheap bands). |
| Opaque `.env*` | **17 paths** unread | Path presence only — never open/quote/log contents. |
| Must-preserve | **Draft Medium** caution | **draft — not auto-locked / for user review**. |
| SSH origin rewrite | **N/A on this tree** | All HTTPS; Continuity still forbids remote URL rewrite unless a later gated step says otherwise. |

**Clearance for whole-tree archive:** **NO** — hazards classified, not cleared.

---

## Recommended fate

| Unit | Fate | Rationale |
| --- | --- | --- |
| `…\TestNewWorkspaceAgent\_backups` (whole parent) | **Isolate** (preferred) / keep / defer — **do not delete by default** | Labeled backup; dual-remote hermes clone; opaque `.env*`; ~1.6 GB. |
| `…\TestNewWorkspaceAgent\_quarantine` (whole parent) | **Isolate** (preferred) / keep / defer — **do not delete by default** | Labeled quarantine; hermes clone HEAD matches backup; dual-remote + opaque `.env*`. |
| Live primary roots `#1–#5` | **Keep in place** | Isolation of clones does **not** clear live multi-remote; whole-tree OOS. |
| Whole `C:\Project\WorkSpace` → dated archive | **OUT OF SCOPE** | Never claim Multi-experiment “solved” via ordinary whole-tree archive while hazards remain. |

Isolation (when later approved) means relocating **named parent trees** intact — not merging into live hermes, not remote URL rewrite, not history rewrite.

---

## Safe relocation / isolation rules

For any **future** plan-gated cycle (scoped or otherwise):

1. **Atomic nested git** — move intact trees that contain `.git`; never strip history / `filter-repo` / force-push.
2. **Named parent units only for scoped FS** — prefer whole `_backups` and/or `_quarantine` parents; do not cherry-pick empty `.git` shells.
3. **Opaque `.env*`** — path presence only; never read/quote/log contents; list unread paths in intent-preview.
4. **Remotes path-only** — inventory names + schemes; **no** `git remote set-url` / origin rewrite unless a later gated step explicitly says so (default: document only).
5. **No linked-worktree surprise** — re-run `worktree list` immediately before any approved move; if >1 worktree appears, fail-closed.
6. **Windows lock recovery** — prefer a single move; on PermissionDenied/split → reunify + `robocopy /E /MOVE`; remove **only empty** leftover `.git` shells; re-attest; log deviation.
7. **Whole XL `WorkSpace` → ordinary Multi-experiment archive** — **OUT OF SCOPE** until dedicated clearance; never claim “solved” that way under ordinary Continuity.
8. **Consent** — Continuity / Choose ≠ plan-gate approval; any `fs_mutation` needs a separate gate + intent-preview.
9. **Row honesty** — after docs-only or scoped isolation, Multi-experiment stays **in progress** with **Remaining: `WorkSpace` only** while the XL tree (and live multi-remote) remain; **never** mark Complete or jump Primary next / Special git.
10. **Peers** — never reopen Medium / Early or re-propose archived `PWAExemple` / `GitTest` / `WorkStationPWA` as sources.

---

## How a future cycle proceeds

1. **Default next:** keep locking Multi-experiment / **`WorkSpace` only**; research+docs or dedicated git-strategy Continuity as needed — **not** soft-deferred force-finalize.
2. **Optional scoped isolation** of named `_backups` / `_quarantine` parents: set `mutation_class: fs_mutation`, full “What the user is approving,” wait for **separate plan-gate yes**. Expect nested count **7→5** and ~3.3 GB out of the live tree **if both** move — **does not** clear live hermes multi-remote; **does not** authorize whole-tree archive; row stays **in progress**.
3. Illustrative from→to draft (non-executed; destinations must be re-confirmed free): see plan Appendix A in [`../sessions/2026.09.10-0907/03-plan/plan.md`](../sessions/2026.09.10-0907/03-plan/plan.md) — **future plan-gate only**.
4. Whole-tree archive remains **blocked** until a dedicated gated strategy clears remaining hazards.

**Cycle 10 did not execute Appendix A or any corpus move.**  
**Cycle 11 did not execute Appendix A or any corpus move** (Continuity Q1=A continue-strategy attestation only).  
**Cycle 12 did not execute Appendix A or any corpus move** (Continuity Q1=A continue-strategy re-probe attestation only).  
**Cycle 13 did not execute Appendix A or any corpus move** (Continuity Q1=A continue-strategy re-probe attestation only).

**Next FAW lock hint:** keep Multi-experiment / **`WorkSpace` only**; default = **continue strategy** from this artifact **or** explicit Continuity **B** + separate plan gate for Appendix A scoped isolation — **not** soft-deferred force-finalize / Complete / Primary next while WorkSpace remains.

---

## Related catalogue pointers

- Index: [`../catalogue/INDEX.md`](../catalogue/INDEX.md) (WorkSpace still at `C:\Project\WorkSpace`)
- Inventory: [`../catalogue/inventory.md`](../catalogue/inventory.md) (nested **7** honesty)
- Must-preserve draft: [`../catalogue/must-preserve.md`](../catalogue/must-preserve.md)
- Roadmap: [`ROADMAP.md`](ROADMAP.md)
